> AIDP 查阅型资料分片（非 @import、不常驻、按需 Read）；总纲见项目记忆文件 `AGENTS.md`（Claude Code 下为 `CLAUDE.md`）的「核心约定」段。

## 初始化输入文件清单

| 类型 | 路径 |
|------|------|
| AIDP 范式 | `docs/init/`（`06_版本与用户目录约定.md` 为路径权威）|
| 产品需求（项目级 PRD，兜底）| `docs/requirements/PRD-{项目名}.md` |
| 产品需求（版本级，★ 推荐）| `docs/requirements/{version}/产品提供/*.md` |
| 研发需求（生成）| `docs/requirements/{version}/研发需求/00_索引.md` + `01_研发需求.md`（多系统则 `00_索引.md` + `01_<系统>.md` / `02_<系统>.md` …；`00_索引.md` 记文件清单 + 生成时间 + 主/补充标识，约定 15）|
| 架构约束 / 技术选型 / UI 规范 | `docs/architecture/架构约束.md` / `技术选型.md` / `UI规范约束.md`（动态填充）|
| 架构设计文档（可选，项目级整体架构）| `docs/architecture/架构设计.md`（单文档）或 `docs/architecture/架构设计/`（多文档子文件夹，`00_索引.md`+`NN_*.md`）；存量任意命名兼容识别，读时全目录扫描（详见 `06_版本与用户目录约定.md` §2.1.1）|
| 第三方接口（跨版本通用全接口文档）| `docs/references/`（根）|
| 第三方对外需求（版本专属：提给第三方的需求 + 本版本对方资料）| `docs/references/{version}/`（规划期生成、强制被研发需求/详细设计引用；发布期 `/version` 整理 + 已满足加说明；详见 `06_版本与用户目录约定.md` §2.6）|
| 版本设计 | `docs/design/detail/{version}/*.md` |
| ★ 高保真原型 | `docs/prototype/{version}/mockup/{module}/`（视觉最高优先级）|
| 原型代码 | `docs/prototype/{version}/code/{module}/`（功能+交互参考）|
| 部署资产 | `docs/deployment/{version}/`（部署文档根；**固定命名契约**见 06 §2.5.4：`00_索引.md`（本版部署产物清单 + **双轨场景导航**）+ `sql/增量/NN_*.sql`（**增量轨**；⛔ `sql/` 根下不得直放 `.sql`，回检 `verify.py::check_deployment_two_track_layout`）+ **`sql/全量/` + `配置文件/全量/`**（★ **全量轨**，约定 37，`/version` 发布期 Step 3.3.7.9 产出，全新部署基线，机器门 = `.aidp/scripts/release_baseline_check.py`）+ **`部署流程/部署流程.md`**（精炼有序 SOP）+ **`部署流程/SQL执行台账.md`**（各环境 SQL 执行状态，团队共享入库）+ `配置文件/增量/配置项清单.md`。**维护分工**：`部署流程/`（SOP + 台账）由 **`/sprint-dev` Step X.8（★ 检测驱动，独立于配置变更——sql/ 有脚本 / 有部署后回填接口 / 需重启即触发，不受 Step X.7「配置文件被改动」门约束）** + `/sprint-bugfix` 维护；`配置文件/` 由 `/sprint-dev` Step X.7 维护。缺失回检 = `verify.py::check_deployment_docs`（本版有 SQL 却缺部署流程 → 部署阻断风险 WARN）；★ 另有 `docs/deployment/tools/`（**跨版本**运维 / 一次性工具归档，**非版本隔离**，按归属应用分 `本应用/` `<上游系统>/` `其他/` + `README.md` 索引，中文文件名 + 日期前缀；`/version` 发布纳入整理，详见 06 §2.5.4.1）|
| 环境配置 | `env/.env` |
| ★ 报告模板 | `.aidp/templates/reports/`（AI执行报告 + AI测试报告 + AI数据清理 + 版本测试报告 + README）|
| ★ 部署模板 | `.aidp/templates/deployment/`（`部署流程.md` + `SQL执行台账.md`；由 `/sprint-dev` Step X.8 检测驱动复制填充到 `docs/deployment/{version}/部署流程/`）|
| ★ AI 全自动报告 | `docs/reports/{version}/AI执行报告/`（autopilot 生成）+ `AI测试报告/`（aiauto-test 生成）+ `AI数据清理/`（每 build 一份数据清理文档，aiauto-test 生成、**仅 autopilot 驱动才产**，含测试环境+要清理的库+清理 SQL；测试后询问是否执行、无人值守只生成不删）+ `版本测试报告/`（/version 发布生成）；build 号 `{version}_build{N}` 详见报告模板 README |

---

## AIDP 范式文档索引

| 文档 | 定位 | 内容 |
|------|------|------|
| [`00_AIDP范式主文档.md`](../../docs/init/00_AIDP范式主文档.md) | 范式骨架 | 目录结构、流程总览、初始化决策树 |
| [`01_初始化输入指导.md`](../../docs/init/01_初始化输入指导.md) | 输入指导 | 项目初始化输入详细说明 |
| [`02_迭代输入指导.md`](../../docs/init/02_迭代输入指导.md) | 输入指导 | 各阶段迭代输入与四种执行模式 |
| [`03_memory文件详细规范.md`](../../docs/init/03_memory文件详细规范.md) | 文件模板 | Memory Bank 文件模板与规范 |
| [`04_agents详细规范.md`](../../docs/init/04_agents详细规范.md) | 索引总览 | Agent 职责边界、协作流程、激活规则 |
| [`05_commands详细规范.md`](../../docs/init/05_commands详细规范.md) | 索引总览 | 命令层次、四种执行模式、统一前置流程 |
| [`06_版本与用户目录约定.md`](../../docs/init/06_版本与用户目录约定.md) | **权威来源** | 版本+用户目录层级的权威规则 |
