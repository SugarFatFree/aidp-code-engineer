# 执行进度 tasks.md

> 进度状态机单一真相。状态:`[ ]`待执行 / `[>]`执行中 / `[√]`已完成(pass) / `[!]`阻塞或失败(fail/block)。
> 只存 用例 ID + 名称,详细内容执行时按 TC-ID 从用例文档现查。按 `## {模块名}` 分组。
> 可由 `python3 <SKILL_DIR>/scripts/tasks_state.py init <用例路径> -o tasks.md` 自动生成。

## SUITE-USER 用户管理(管理员视角)

- [ ] TC-USER-001 - 新增用户成功
- [ ] TC-USER-002 - 用户名必填校验
- [ ] TC-USER-003 - 用户列表列集合逐列核对

## SUITE-ORDER 订单管理

- [ ] TC-ORDER-001 - 创建订单成功
- [ ] TC-ORDER-002 - 订单状态流转(草稿→待支付→已支付)
- [ ] TC-ORDER-003 - 已完成订单不可编辑(非法迁移被拒)

<!--
状态流转示例:
  批量执行前整模块 [ ]→[>];执行完据结果 [>]→[√] 或 [>]→[!]。
  续跑:tasks_state.py resume 列出所有 [ ]/[>],已 [√] 跳过,不从头重来。
  安全检查点:checker 补齐至无 [ ]/[>] 残留,方可出报告。
-->
