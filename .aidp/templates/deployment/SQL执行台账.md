# {version} SQL 执行台账

> 本版本各环境 SQL 脚本执行状态台账——**团队共享入库**（区别于 per-user `memory/{version}/{user}/.applied-sql.json`）。
> 由 `/sprint-dev` Step X.8 随开发建立并登记「开发库」列（约定 6 应用开发库 SQL 成功后同步登记）；演示/生产列留 `待部署` 占位，由各环境部署时补填。
> 只记执行状态与影响行数，**不复述 SQL 内容**（内容见 `../sql/`）；不记任何库口令。

## 一、脚本清单与各环境执行状态

> 状态图例：`✅ 已执行` / `⏳ 待部署` / `⛔ 失败` / `➖ no-op（幂等跳过）`。影响行数按实际填；no-op 在「备注」列记原因（如"表已存在、IF NOT EXISTS 跳过"）。

| # | SQL 脚本（`../sql/`）| 用途 | 开发库 | 演示环境 | 生产环境 | 备注 |
|---|---|---|:--:|:--:|:--:|---|
| 01 | `01_*.sql` | {建表/字典} | ⏳ | ⏳ | ⏳ | {影响行数 / no-op 原因} |
| … | … | … | ⏳ | ⏳ | ⏳ | |

## 二、执行前预检

| 环境 | 预检项 | 结果 | 检查时间 |
|------|--------|------|---------|
| 开发库 | 目标库可达 + 账号有权限 + 备份已做 | {待填} | {YYYY-MM-DD HH:MM} |
| 演示环境 | 同上 | 待部署 | |
| 生产环境 | 同上（★ 生产必须先备份） | 待部署 | |

## 三、执行后核对

| 环境 | 核对项（关键表行数 / 字典完整性 / 迁移结果） | 结果 | 核对时间 |
|------|------|------|---------|
| 开发库 | {如：菜单表 N 行、权限表 M 行、迁移接口回填 K 行} | {待填} | |
| 演示环境 | 同上 | 待部署 | |
| 生产环境 | 同上 | 待部署 | |

### 三之二、★ 本版新增列注释覆盖率（**实查库、不看脚本**；本版有 `ADD COLUMN` 时必填）

> ⛔⛔ **为什么必须实查库、而不是核对脚本**（真实下游事故）：SQL 脚本里
> **写了 `COMMENT ON COLUMN`**，但手工执行时只跑了 `ALTER TABLE ADD` 那一段循环，
> 注释语句整段被漏掉——**而没有任何一步会发现**。事后实查 `ALL_COL_COMMENTS`：新增 10 列
> **10/10 无注释**。**脚本内容正确 ≠ 库里状态正确**，这道核对查的就是后者。
>
> **覆盖率必须 100%，缺一即 Critical、不得把本行标成"已完成"。** 无 `ADD COLUMN` 的版本填「本版无新增列」。

```sql
-- 达梦 / Oracle：本版新增列的注释实查（{TABLE}/{COL1},{COL2} 换成本版实际值）
SELECT c.COLUMN_NAME, cc.COMMENTS
  FROM ALL_TAB_COLUMNS c
  LEFT JOIN ALL_COL_COMMENTS cc
    ON cc.OWNER = c.OWNER AND cc.TABLE_NAME = c.TABLE_NAME AND cc.COLUMN_NAME = c.COLUMN_NAME
 WHERE c.TABLE_NAME = '{TABLE}' AND c.COLUMN_NAME IN ('{COL1}','{COL2}');
-- MySQL / PostgreSQL 等：查 information_schema.COLUMNS 的 COLUMN_COMMENT / col_description
```

| 环境 | 本版新增列（逐列列出） | 有注释 / 总数 | 缺注释的列名 | 核对时间 |
|------|------|:--:|------|---------|
| 开发库 | {如 `T_ENTERPRISE.STATUS`、`T_ENTERPRISE.ADMIN_USER_ID`} | {2/2} | {无} | {YYYY-MM-DD HH:MM} |
| 演示环境 | 同上 | 待部署 | | |
| 生产环境 | 同上 | 待部署 | | |

> 📌 **附带报告（不阻塞本版）**：顺手记一下该表**已有**的无注释列（典型是
> `CREATE_TIME`/`UPDATE_TIME`/`CREATE_BY`/`UPDATE_BY` 这类审计字段）——属存量欠账，
> 值得可见但**不该拦住本版发布**，另行立项补。
>
> 注释内容要求：**业务含义 + 取值域（枚举必须列全）+ 与近似列的区别**（若设计期的重复性对账命中过）。

## 四、部署后必须执行的接口/脚本（数据初始化 / 历史迁移）

> 与 `部署流程.md`「关键部署项」的初始化接口/数据迁移一致；此处记各环境实际调用状态。无则填"无"。

| 接口/脚本 | 用途 | 开发库 | 演示环境 | 生产环境 | 备注 |
|----------|------|:--:|:--:|:--:|---|
| {如 `POST /internal/course/rebuild-shelf-state`} | {回填课程上下架状态} | ⏳ | ⏳ | ⏳ | {回填行数} |
