# 代码验收报告

> 判「过没过」读 `--json` 的 `gate_passed`；`skipped=true` = Web 机器子项未扫到前端源码、**不等于维度 9 通过或整维度跳过**;非 Web 通用半场仍需核,如实标「Web 子项跳过（原因）」。
## 14. 客户端应用 MCP 业务能力实现合规（维度 9·**条件启用**）

> `client_mcp.enabled` 与旧 Web `webmcp_enabled` 均未启用时把本节整节删除,不留 N/A 行;只由调用方入参门控,**测试驱动 MCP** 可用不能推断应用已提供工具。新旧声明冲突明确记不通过。
## 10b. 同族增量项一致性（维度 15·被测项目 约定 20）

> ⚠️ **编号是 `10b` 而不是往后顺延**——刻意不动 11~14 的既有编号（下游按编号引用本模板，顺延会让每一条引用整体位移，与 `4b` / `6b` 同款处置）。**本行不得删除**：维度 15 在 SKILL.md 里要求「报告仍留行」，删掉这一节等于那句承诺无处落地。
>
> **两种跳过情形都标「⏭️ 不适用 + 原因」并保留本节**：① 被测项目未随脚手架下发
> `<被测项目根>/{{AIDP_HOME}}/scripts/check_sibling_family.py`；② 本轮确实没有任何同族增量项。
> ⚠️ **情形 ① 只关掉机器半场（F0~F4），15.1~15.3 的语义核对照跑**——它们不依赖脚本。
> **退出码 2 是入参/环境错，不是维度违规。**

```bash
python3 <被测项目根>/{{AIDP_HOME}}/scripts/check_sibling_family.py --json
# 退出码 0=无 Critical / 1=有 Critical / 2=入参或环境错
```

| 判据 | 状态 | 说明 |
|--------|------|------|
| F0 声明漂移 | ✅ / ❌ / ⏭️ | `SIBLING-FAMILY:` 声明与实际成员/外壳/排序数据源对不上 |
| F1 成员自带外壳 | ✅ / ❌ / ⏭️ | 成员里有本该由公共单元产出的结构/样式 |
| F2 成员未登记 | ✅ / ❌ / ⏭️ | 新成员没进注册表 / 排序数据源 |
| F3 顺序未数据化 | 🟡 / ⏭️ | Important，不阻断；顺序由书写位置决定即命中 |
| F4 疑似家族无公共外壳 | 🟡 / ⏭️ | Important，不阻断 |
| 15.1 结构骨架一致 | 🟡 / ✅ / ⏭️ | 与任取 2 个既有成员并排读：区块层级、插槽用法、标题/说明/操作区构成是否同形 |
| 15.2 文案骨架一致 | 🟡 / ✅ / ⏭️ | **查段数与要素齐不齐，⛔ 不查措辞是否雷同** |
| 15.3 交互流一致 | ✅ / ❌ / ⏭️ | 二次确认 / loading / 成功失败反馈 / 禁用条件；**最高频形态：其他成员都有二次确认，新成员直接执行** |
| 上游契约消费 | ✅ / 🟡 / ⏭️ | 有无 architect 检查项 41「同族增量项声明表」；无则回退纯代码判断并报 🟡「设计缺声明表」，不静默放过 |

> ⚠️⚠️ **只约束外壳与骨架，⛔ 不约束内容**——成员之间的内容差异是正常的，
> 判不通过的只能是「**该有的那一层没有**」，不能是「**写得和隔壁不一样**」。

---

## 0. 静态基线门（维度 0 · 前置，不计入 15 维度）

> 非零退出即直接回 Agent1 修复、不往下走 15 维度。工具缺失时如实标「⏭️ 跳过（工具缺失：<原因>）」，
> 验收时间：{日期}
> 验收范围：{功能/模块名称}
> 验收轮次：第 {N} 轮
> 验收结果：✅ 通过
> 输入文档：{列出使用的 PRD、设计文档、接口文档}

---

## 0. 静态基线门（维度 0 · 前置，不计入 14 维度）

> 非零退出即直接回 Agent1 修复、不往下走 14 维度。工具缺失时如实标「⏭️ 跳过（工具缺失：<原因>）」，
> **不得**改用会联网安装的裸 `npx`，也不得升级为全量 build。

| 项 | 命令 | 结果 |
| :- | :- | :- |
| 类型检查 | `npx --no-install vue-tsc --noEmit` / `tsc --noEmit` | ✅ 通过 / ⏭️ 跳过（工具缺失：…） / 🔴 N 处错误 |
| lint（本次改动文件） | `npx --no-install eslint <改动文件>` | ✅ 无 error / 🟡 N 个 warning（不阻断） / 🔴 N 个 error |
| Java / Spring | 本档不做（`mvn compile` 有 lifecycle 副作用，见 references/dimension-0-2-baseline-mock.md 维度 0） | ⏭️ N/A |

**存量基线（首轮填写）：** 类型错误存量 __ 处 —— 本轮只对**增量**判 Critical。

## 1. 功能完整性检查（维度 1）

| 功能点/Bug | 状态 | 说明 |
|-----------|------|------|
| {功能点 A} | ✅ 通过 | {验证说明} |
| {功能点 B} | ✅ 通过 | {验证说明} |

---

## 2. Mock 数据清除检查（维度 2A / 2B）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 硬编码假数据 | ✅ 无残留 | 扫描 src/ 目录，未发现 mockData/fakeData 等关键词 |
| mock 目录 | ✅ 已清空 | 无 mock/mocks/__mocks__ 目录 |
| 假数据函数 | ✅ 无残留 | API 层无 Promise.resolve(fakeData) 等模式 |
| mock 工具 | ✅ 无残留 | 无 Mock.js/MSW/json-server 引用 |
| 模拟静态对象 | ✅ 无残留 | 组件中无模拟接口返回的静态对象 |
| TODO/FIXME 标记 | ✅ 无残留 | 无"替换为真实接口"等注释 |
| mock 兜底逻辑 | ✅ 无残留 | catch 块中无回退到假数据的逻辑 |
| 第三方临时 mock（2B，如适用） | ✅ 合规 | THIRD_PARTY_MOCK 标注完整、运行时可控、无并存/兜底/过期/真实对接后残留 |

---

## 3. 真实接口对接检查（维度 3）

| 接口名称 | Method + URL | URL | 方法 | 参数 | 返回 | 状态码 | 错误处理 | 结果 |
|---------|-------------|-----|------|------|------|--------|---------|------|
| {接口A} | {GET /api/xxx} | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ 通过 |
| {接口B} | {POST /api/xxx} | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ 通过 |

---

## 4. 代码质量评估（维度 4）

| 检查项 | 评估 | 说明 |
|--------|------|------|
| 命名规范 | ✅ 符合 | {说明} |
| 错误处理 | ✅ 完善 | {说明} |
| 类型安全 | ✅ 良好 | {说明} |
| 安全隐患 | ✅ 无发现 | {说明} |
| 性能问题 | ✅ 无发现 | {说明} |
| 金额字段类型守恒 | ✅ 符合 | {Entity/DTO 类型链一致,无 BigDecimal/Double/Float/number 越级} |
| 金额 JSON 序列化 | ✅ 符合 | {超 2^53 或 0 位小数货币按 string 传输} |
| 注释完备性 | ✅ 完备 | {类/接口/枚举/组件/方法/字段均有文档注释,关键代码段有行内注释} |
| 文件复杂度 + 复用封装 | ✅ 符合 | {单文件不超阈值, 单方法 ≤ 50 行, 无 3+ 次裸重复} |
| 缓存代码与设计一致性 | ✅ 符合 | {代码缓存使用与详细设计 A.2「缓存方案」双向一致, 无未授权缓存} |
| 字段/列对账（字段一致性·视觉还原支柱①） | ✅ 符合 | {实现可见列展示名(语义) ⟷ 需求字段清单(语义)一致; 缺/多/改名/换序差异均已在「需求字段→处置」对照表登记, 裁剪不阻塞} |
| 字段集与设计双向比对 | ✅ 符合 | {实现列/字段集合(scan_field_columns) ⟷ 详细设计「字段实现清单」(scan_design_field_inventory)双向一致, 无"设计有实现无"Critical、无"实现有设计无"未登记增列 Important, 展示顺序与设计「顺序」列一致} |
| 产品 PRD 原文 ⟷ 实现 条目对账（顶档） | ✅ 符合 / ⏭️ 跳过 | {以产品原始 PRD 原文为独立基准逐条回检按钮/列/操作/规则是否存在于实现, 逐条标注 PRD 条目号; 未提供产品 PRD 原文时填「⏭️ 跳过（未提供产品 PRD 原文）」} |
| 视觉还原度（L1/L2·样式一致性·视觉还原支柱②） | ✅ 符合 | {L1 核心页与视觉基准(高保真 或 原型 code/+设计令牌)布局/控件/主色一致, 主色/字号/间距/圆角取自原型 token 体系(非组件库默认、非硬编码), L2 无明显非结构偏差} |
| 内容 + 操作逻辑完整性（对齐原型第三层） | ✅ 符合 | {原型可见区块/卡片/次级操作/跳转与交互态(loading/empty/error/success/disabled)均已实现, 校验/联动/条件显隐/增删改流/二次确认/步序等操作逻辑照原型未简化未改流程, 未实现项均在原型内容基线登记裁剪/延期} |
| 文案与数据口径一致性 | ✅ 符合 / ⏭️ 降级 | {scan_stale_copy 无命中: 表 E 处置=改写行的旧口径字样在已改口径展示位的说明文案(tooltip/表头注解/帮助文字/空态提示)中无残留; 表 F 作废行的 REQ 编号在代码注释中均已标注「该需求已于 {version} 作废」。未提供表 E/表 F 时按 SKILL 规则降级并注明} |
| 叶子组件替换白名单（约定28 实现选型层） | ✅ 符合 | {原型叶子小组件换成项目等价件, 主题 token 取自原型体系、操作逻辑/交互行为与原型一致; 白名单外的结构/版心/栅格/内容/操作逻辑均未偏离} |
| 死代码/死引用残留 | ✅ 无残留 | {scan_code_conventions dead-ref 0 命中, 无引用已删旧组件/路由/模块} |
| DB 约束前置校验 | ✅ 符合 | {设计 A.3 各约束(NOT NULL/length/UNIQUE/外键)在 Controller/Service 有前置校验} |
| HTTP 客户端配置硬编码 | ✅ 符合 | {http-hardcode 0 命中, base URL/IP/凭证走配置 + ${ENV} 占位} |
| 配置中心热刷新漏标 | ✅ 符合 | {可变 key 的 @Value 类均带 @RefreshScope / @ConfigurationProperties} |
| 新增依赖/import 越界基线 | ✅ 符合 | {dep-baseline 0 命中, 新增 import 顶层包均已声明在清单} |
| 审计字段填充溯源 | ✅ 符合 | {audit-fill 0 命中, 审计字段走自动填充从登录态取操作人, 无写死 system, 逻辑删除同步 update_by/update_time} |
| 用户自我保护校验 | ✅ 符合 | {用户删除/禁用/降权接口后端以当前登录态身份为基准拦截自伤操作, 非仅前端置灰} |

---

## 4b. 实现偏离设计（维度 13·外部脚本门控；被测项目无 `{{AIDP_HOME}}/scripts/check_design_anchor.py` 或未取到「验收版本号」时标「不适用」跳过、**仍留行**）

> 回检脚本（**在被测项目侧、不在本 SKILL**）：
> `python3 <被测项目根>/{{AIDP_HOME}}/scripts/check_design_anchor.py --version <版本号> --json`
> ⚠️ **`--version` 是必填的带值参数**，与维度 10/12 的「只要 `--json`」不同；漏传会落 `exit 2`（入参错），
> 而 `exit 2` 会被当成「修正参数后重跑」，这一档就**既不算过也不算不过**。⛔ 不得自拟版本号把脚本跑绿。
> **本行不得删除。** 未下发脚本 / 未取到版本号时，把状态栏填成「⏭️ 不适用（未下发 `check_design_anchor.py`）」
> 或「⏭️ 不适用（未取到 `--version` 值，已在报告结论段向调用方点名索要）」，**不是**留空、更**不是**整节删掉。

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 设计字段/常量落点 | ✅ 无未落点锚点 / 🟡 {N} 项已交代 / ⏭️ 不适用（{原因}） | {`check_design_anchor.py --version {版本号} --json` 结果；每个未落点锚点须写明处置：①补齐实现 ②改设计并按约定 22 级联更新 ③行内 `design-anchor-ignore: <理由>` 显式豁免} |

---

## 5. DI 依赖可解析性（维度 6·Java/Spring；非 Java 项目整维度跳过）

> 回检脚本：`python3 <SKILL_DIR>/scripts/check_di_resolvability.py <代码目录> [--json] [--strict] [--config-keys-file <配置项清单>]`
> 非 Java/Spring 项目（扫不到 `.java`）本章填「⏭️ 整维度跳过（非 Java/Spring 项目）」，不计入通过/不通过。

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 注入点可解析性 | ✅ 无 Critical | {扫描 N 个 .java / M 个注入点，Critical 0 项——无「注入框架不自动装配的类型(RestTemplate/WebClient/OkHttpClient/HttpClient)」、无「注入仓内具体类却无组件注解/@Bean」} |
| 多 bean 无 @Qualifier / 接口无实现 | 🟡 Warn {X} 项（不阻断） | {列出 Warn 项及确认结论；Spring 按字段名匹配 beanName 兜底，静态不判死} |
| @Value 键存在性 | ✅ 无缺失 | {K 个占位符，无默认值的键均在 application*/bootstrap* 或配置项清单命中（relaxed binding 忽略 -/_/大小写）} |

---

## 6. CSS 预处理器一致性（维度 7·Vue；非 Vue 项目整维度跳过）

> 补的是 `vue-tsc` 与 `eslint` **都不编译 `<style>` 块**留下的缝：预处理器没装 / lang 写错，
> 开发期轻量验证全绿，CI `vite build` 才炸。纯静态比对，零构建成本。

```bash
python3 <SKILL_DIR>/scripts/check_vue_style_preprocessor.py <改动的 .vue 列表> --json
```

| 检查项 | 结果 | 说明 |
| :- | :-: | :- |
| `<style lang>` 所需预处理器已在 package.json 声明 | ⬜ | 未声明 = 🔴 Critical（CI 构建必失败，回 `/sprint-bugfix`）|
| lang 与全库众数一致 | ⬜ | 孤例 = 🟡 Warn（疑似写错，不阻断；有意混用属合法）|
| 技术栈门控 | ⬜ | 扫不到 `.vue` → 整维度跳过，不计入通过/不通过 |
| `@import`/`@use` 被引文件确实存在 | ⬜ | **脚本不覆盖**（别名 `@/`、`~`、sass partial 省略规则叠加后静态解析误报率高）——本次改动新增了 `@import`/`@use` 时须**人工核对**，未新增填「不适用」|

**全库众数 lang**：`____`（分布：`____`）

> 判「过没过」读 `--json` 的 `gate_passed`（`--strict` 下 WARN 也计入），`passed` 恒只看 ERROR。

---

## 6b. 样式重构编译等价性对照（维度 14·改动了样式时才触发；未改样式 / 预处理器未安装时标「不适用」跳过、**仍留行**）

> ⚠️ **编号是 `6b` 而不是往后顺延**——刻意不动 7~14 的既有编号（下游按编号引用本模板，顺延会让每一条引用整体位移）。**本行不得删除**：维度 14 在 SKILL.md 里要求「报告仍留行」，删掉这一节等于那句承诺无处落地。
>
> **通用静态核对(所有已声明端,只判一次):** 应用自有实现及工具 Schema/身份与权限/写操作确认/生命周期/错误/审计日志有源码落点。非 Web 未证实服务/桥接不得用 Appium/小程序驱动替代;实现未取到不判通过。

| 通用检查 | 结果 | 证据 |
| :- | :-: | :- |
| 应用业务工具声明与实现/来源 | ⬜ | {客户端/实现形态/文件或服务} |
| 工具契约、权限、确认、错误与审计 | ⬜ | {逐工具落点及设计锚点} |

> **以下 Web 专项只执行一次**:仅 Web+WebMCP 时保留下方脚本与 9.1~9.6 表;通用半场只补 Web 清单未覆盖项(声明/Schema 来源/审计落点),不重复登记确认/错误/权限问题。非 Web 删除 Web 子表但保留本维度通用结论。原 `check_webmcp_adapter.py` 不接受 App/小程序/桌面源码。
> 补的是维度 7 覆盖不到的另一半：7 管**预处理器有没有装**，14 管**样式重构后产物变没变**。「类型检查过了」对样式**毫无意义**，是使用者最容易误采信的一句话。

```bash
# 1) 取基线并编译（命令随项目而定，用被测项目自己的编译器，⛔ 不联网装、不全局装）
git show HEAD:<样式文件> > /tmp/old.scss && ./node_modules/.bin/sass --no-source-map /tmp/old.scss /tmp/old.css
./node_modules/.bin/sass --no-source-map <样式文件> /tmp/new.css
# 2) 归一 + 比较（含基线有效性判定）
python3 <SKILL_DIR>/scripts/check_css_equivalence.py /tmp/old.css /tmp/new.css --json
```

| 检查项 | 结果 | 说明 |
| :- | :-: | :- |
| 基线有效（非空、无「未执行」信号） | ⬜ | 无效 = `exit 2` **拒绝比较**；报告写「基线无效，本项未核验」，**不是通过** |
| 编译产物零差异 | ⬜ | 有差异 = 🔴 Critical，除非**逐条**由本次改动意图解释并写进下方说明 |
| 仅顺序变化 | ⬜ | 🟡 Important——CSS 有**层叠**语义，需逐条确认这些块之间没有覆盖关系 |
| 公共样式文件的文档注释用 `//` 而非 `/* */` | ⬜ | 块注释**会被编译进产物**（下游实测污染 29 行） |
| 公共 mixin 无「无条件输出的多余声明」 | ⬜ | 下游实测：一条无条件 `margin` 给原本没有它的调用方**凭空加上** |

**差异逐条说明**（若非零差异）：`____`

> **前端最低静态验证组合 = 类型检查（带阳性对照）+ lint（同文件集基线对照）+ 预处理器编译（改样式时）**，三者盲区互不重叠、缺一不可。⚠️ 「类型检查带阳性对照」不是废话：`vue-tsc` 会因 `tsconfig` 的 `types` 指向未安装的包而**提前中止**，**源码零检查却报「0 错误」**——跑之前先故意注入一个类型错误，确认它能红。

---
## 7. 请求通道 URL 拼装单一信源（维度 8·前端；**无前端源码**的项目整维度跳过）

> 补的是「类型检查与 lint 全绿、只有真实发请求才暴露」的运行期缝：同一 URL 判据散落多份、
> 各写各的，第 N 份重踩同坑（典型：接口常量已含 context-path 又被拼一次 base → 路径重复 → 404）。
> **本维度不判某一处拼得对不对**（静态拿不到 env 值），只判**这条判据有没有平行实现**。

```bash
python3 <SKILL_DIR>/scripts/check_request_channel_url.py <前端目录> --changed <本次改动的文件...> --json
```

| 检查项 | 结果 | 说明 |
| :- | :-: | :- |
| base 拼装判据只有一处实现 | ⬜ | 散落 ≥2 个文件 = 平行实现 → 🔴 Critical（回 `/sprint-bugfix` 收敛到单一信源）|
| 本次改动是否新增了一份平行拼装 | ⬜ | 传 `--changed` 后，只有落在改动文件里的判 Critical，存量降 🟡 Warn |
| 技术栈门控 | ⬜ | 扫不到任何前端源码 → 整维度跳过，不计入通过/不通过。**注意不限 Vue**——React / 原生 TS 前端同样执行；**Node 服务端（`.ts/.js`）也不跳过**，只有 Java·Go·Python 这类无前端源码的后端才跳过 |
| 拼出来的 URL 到底对不对 | ⬜ | **脚本不覆盖**（静态拿不到 env 值）——base 该不该含 context-path 属**维度 3**，本章不重复登记 |

**认定的单一信源**：`____`（`single_source` 字段）
**未计入的非请求通道用法**（`ignored_no_request_call`）：`____`
  ↑ 页面跳转 `location.href`、`new HttpClient(base)` 传参、`resolvePath(base,url)` 这类合法形态，列出供人扫一眼；
  ↑ 也包含「拼装在本文件、请求发起点在别的文件」这种被条件 ② 击穿的真命中，**别只扫不看**
**未参与检查的文件**（`unreadable_files`）：`____`
  ↑ 权限不足/断链符号链接，**不等于干净**，需人工确认

> 判「过没过」读 `--json` 的 `gate_passed`。
> ⚠️ 单一信源是**文件级**判定：同一文件内写了多份平行判据脚本不判，需人工看一眼该文件。

---

## 8. UI 还原度确定性检查（维度 10·约定39-R2/R3/R10；被测项目无 `{{AIDP_HOME}}/scripts/check_ui_fidelity.py` 时标「不适用」跳过、**仍留行**）

> ⛔ **本 SKILL 不得自带同判据实现**——脚本由 AIDP 脚手架下发到被测项目侧。退出码 `2` 是入参/环境错、
> **不是维度违规**。`fidelity-ignore` 豁免只对 R2/R3 有效，**R10 零豁免**；只写规则号不写原因的豁免须报 Important。

| 规则 | 状态 | 说明 |
|--------|------|------|
| 约定39-R10 导出透传分页参数（🔴 零豁免） | ✅ / ❌ / ⏭️ 不适用（原因） | findings 中 R10 命中数 |
| 约定39-R3 截断无 tooltip（🟡） | ✅ / 🟡 / ⏭️ | 命中数 + 已声明豁免数（须带原因） |
| 约定39-R2 状态标签颜色字面量（🟡） | ✅ / 🟡 / ⏭️ | 命中数 |

---

## 9. 失效实体与边界（维度 11·约定39-R5~R9 + R12·纯静态语义核对）

> 某条本轮**确实无对应场景**时标「⏭️ 不适用 + 一句话理由」，**不得直接省略**——省略与"查过且通过"
> 在报告里长得一模一样。

| 规则 | 状态 | 说明 |
|--------|------|------|
| 约定39-R5 失效实体操作入口拦截 | ✅ / ❌ / ⏭️ | 渲染处是否隐藏或禁用（非点击后报错） |
| 约定39-R6 被删引用降级为可读展示 | ✅ / ❌ / ⏭️ | 是否裸露 code/ID/空值；有无快照名 |
| 约定39-R7 加载态不渲染脏数据 | ✅ / ❌ / ⏭️ | 初始值是否预置假数据 + 有无加载守卫 |
| 约定39-R8 编辑成功后视图自动同步 | ✅ / ❌ / ⏭️ | 含跨组件场景（详情改了列表刷没刷） |
| 约定39-R9 存量数据兼容性 | ✅ / ❌ / ⏭️ | 新必填/收窄取值的存量处置 |
| 约定39-R12 同一指标跨页面同源 | ✅ / ❌ / ⏭️ | 取数点是否散落多处；对照详细设计「统计指标口径表」权威口径 |

---

## 10. 上游调用日志可见性与脱敏（维度 12·约定40）

> **⚠️ 全程静态**：本节查的是**代码里有没有打日志的语句、打了什么、脱敏在哪一层**，
> ⛔ 不是把服务跑起来看日志输出（跑起来读实际日志归 `/sprint-aiauto-test`）。
>
> **双重门控，两种情形都标「⏭️ 不适用 + 原因」并保留本节**：① 被测项目未随脚手架下发
> `<被测项目根>/{{AIDP_HOME}}/scripts/check_upstream_call_log.py`；② 非 JVM 栈（脚本只扫 `.java`/`.kt`，
> `outbound_files: 0`）。**不得整节删掉**——那是维度 9 的待遇，本节留行才能让阅读者知道
> "这一档没跑过"。**退出码 2 是入参/环境错，不是维度违规。**

```bash
python3 <被测项目根>/{{AIDP_HOME}}/scripts/check_upstream_call_log.py --json
# 退出码 0=无 Critical / 1=有 Critical / 2=入参或环境错
```

| 判据 | 状态 | 说明 |
|--------|------|------|
| C1 出站调用类零日志 | ✅ / ❌ / ⏭️ | 脚本 findings 中 C1 数量；填 `scanned_files` / `outbound_files` 实测值 |
| C2 成功路径不可见（全为 warn/error） | ✅ / ❌ / ⏭️ | **本维度核心**：只有出事才出声 = 不通过 |
| I1 无一条日志提到 URL | 🟡 / ⏭️ | Important，不阻断 |
| I2 成功路径只有 debug | 🟡 / ⏭️ | Important，不阻断 |
| I3 疑似凭据明文入日志 | 🟡 / ⏭️ | Important，但须逐条人工确认后再定性 |
| 12.1 脱敏是否覆盖嵌套结构 | ✅ / ❌ / ⏭️ | 脱敏须发生在**序列化环节**，不是只在最外层拼串时替换 |
| 12.2 二进制降级是否只打元信息 | ✅ / ❌ / ⏭️ | 文件名 + 大小 + contentType；禁 base64 / `new String(bytes)` 入日志 |
| 12.3 拦截器是否覆盖全部出站客户端 | ✅ / ❌ / ⏭️ | 出站客户端个数 **vs** 拦截器注册到的 Bean 个数，差集逐个点名；**抵扣了哪些以脚本 `--json` 的 `waived` 字段为准**，勿凭记忆假设 |
| 上游契约消费 | ✅ / ❌ / ⏭️ | 有无 architect「上游调用日志与脱敏声明表」；无则回退纯代码判断并报 🟡「设计缺声明表」，不静默放过 |

---

## 11. 优化建议

| 序号 | 类型 | 位置 | 建议 | 优先级 |
|------|------|------|------|--------|
| 1 | {性能/代码/架构} | {文件:行号} | {具体建议} | 🔵 建议 |

---

## 12. 技术风险识别（维度 5）

| 风险等级 | 风险描述 | 影响范围 | 缓解建议 |
|---------|---------|---------|---------|
| {高/中/低} | {风险描述} | {影响范围} | {缓解建议} |

---

## 13. 技术债识别（维度 5）

| 序号 | 类型 | 位置 | 描述 | 建议处理时间 |
|------|------|------|------|------------|
| 1 | {TODO/硬编码/过时依赖/架构耦合} | {文件:行号} | {描述} | {建议处理时间} |

---

## 14. WebMCP 前端能力实现合规（维度 9·**条件启用**）

> ⚠️ **未启用时（`webmcp_enabled` 为 `false` 或未传）请把本节整节删掉** —— 不留标题、不写
> 「N/A」、不写「本项目不适用」。**标 N/A 也算在报告里留位置**，而「不占篇幅、不留位置」是
> 调用方给的硬前提（这一点严于维度 6/7/8：那三个未命中技术栈时会留一行「跳过」）。
> **本节刻意排在最后一个编号章节**，删掉不会留下编号空洞。
>
> 门控由**调用方入参**决定，不由技术栈决定。⛔ **验收 Agent 严禁自行探测是否启用。**

```bash
python3 <SKILL_DIR>/scripts/check_webmcp_adapter.py <前端目录> --entry-symbols <调用方传入的符号...> --json
```

| 检查项 | 结果 | 说明 |
| :- | :-: | :- |
| 9.1 单一适配层（命中文件数 == 1） | ⬜ | 散落 ≥2 个文件 → 🔴 Critical。**注释里写了也算命中**（否则 grep 形同虚设）|
| 9.2 无「先登记后撤销」时序 | ⬜ | 登记不得早于配置就绪（**人工核**）；代码出现 `unregisterTool` → 🔴 Critical（**脚本核**：该 API 实测不存在，臆想它存在会让撤销静默失效）|
| 9.3 错误契约无死文案 | ⬜ | 🟡 Important。新增文案时须 grep 旧文案**全部使用点**，只改一处会留姊妹路径（**人工核**）|
| 9.4 写操作复用业务既有执行通道 | ⬜ | 🔴 Critical。另写一条 → 「手工能通、让助手做就不通」，两条链路日志/校验/变量替换各不相同（**人工核**）|
| 9.5 敏感值不经工具通道外泄 | ⬜ | 🔴 Critical。凭据/密钥/令牌**含脱敏片段**一律不返回，只给名称/位置/作用域（**人工核**）|
| 9.6 可调用对象用白名单而非黑名单 | ⬜ | 🔴 Critical。**列表、详情、执行三处须共用同一判据，任一处放行即构成绕过**（**人工核**）|

**认定的单一适配层**：`____`（`single_source` 字段；`--allow` 模式下回落为那个唯一已声明信源）
**入口标识符**（调用方 `webmcp_entry_symbols` 传入）：`____`
**告警明细**（`warns`，建议直接带 `--strict` 跑）：`____`
  ↑ 含 `skipped-large`（超限未扫）/ `unreadable`（不可读）——**都不等于干净**，里面若有第二处
    适配层或 `unregisterTool` 一律看不见，须人工确认；`--strict` 下它们会直接卡住闸门

> 判「过没过」读 `--json` 的 `gate_passed`；`skipped=true` = 扫不到前端源码、**不等于通过**，如实标「跳过（原因）」。
> ⚠️ **脚本只做 9.1 与 9.2 的 `unregisterTool` 两项，其余四项须人工静态核对——脚本全绿 ≠ 本维度通过。**

---

## 验收结论

本次验收共检查 {N} 个功能点、{M} 个接口对接、{K} 项代码质量指标，全部通过。
共提出 {X} 条优化建议、识别 {Y} 项技术风险、{Z} 项技术债。
