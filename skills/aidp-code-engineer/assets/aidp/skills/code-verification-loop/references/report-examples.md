# code-verification-loop · Step 4 验收结果输出格式示例

> 本文件是 `SKILL.md` 的按需加载分片：执行到对应维度 / 步骤时 Read。通用判据（严重度定义、三方冲突裁决、最高约束「全程静态」、基线对照有效性铁律）以 `SKILL.md` 为准，本文件不重复。

**输出格式示例（验收通过）：**

```markdown
## 代码验收结果 ✅ 通过

**验收范围：** 用户管理模块
**验收轮次：** 第 1 轮

### 1. 功能完整性检查（维度 1）
| 功能点 | 状态 | 说明 |
|--------|------|------|
| 用户列表分页 | ✅ | 分页参数正确，数据展示正常 |
| 用户创建表单 | ✅ | 表单校验完善，提交成功 |

### 2. Mock 数据清除检查（维度 2A / 2B）
| 检查项 | 状态 |
|--------|------|
| 硬编码假数据 | ✅ 无残留 |
| mock 目录 | ✅ 已清空 |
| mock 工具 | ✅ 无残留 |

### 3. 真实接口对接检查（维度 3）
| 接口 | URL | 参数 | 返回 | 状态码 | 错误处理 | 结果 |
|------|-----|------|------|--------|---------|------|
| 用户列表 | GET /api/user/list | ✅ | ✅ | ✅ | ✅ | ✅ |
| 创建用户 | POST /api/user | ✅ | ✅ | ✅ | ✅ | ✅ |

### 4. 代码质量（维度 4）
| 检查项 | 状态 | 说明 |
|--------|------|------|
| 命名/错误处理/类型安全/安全/性能 | ✅ | 符合规范 |
| **错误契约与失败可见性（fail-closed）** | ✅ | 失败分支内无「成功响应表达失败」；前端 catch 区分空数据/取数失败；安全判据 fail-closed；解析失败不静默落默认值 |
| 金额字段类型守恒 | ✅ | Entity/DTO 用 Long, 无 BigDecimal 越级 |
| 金额 JSON 序列化 | ✅ | 常规金额 integer, 超 2^53 用 string |
| 注释完备性 | ✅ | 类/方法/字段/关键段注释齐全 |
| 文件复杂度 + 复用封装 | ✅ | 单文件 < 阈值, 单方法 ≤ 50 行, 无 3+ 次裸重复 |
| 缓存代码与设计一致性 | ✅ | 代码缓存使用与详细设计 A.2「缓存方案」双向一致 / 无未授权缓存 |
| 字段/列对账（字段一致性·视觉还原支柱①） | ✅ | 实现可见列展示名(语义) ⟷ 需求字段清单(语义)一致, 缺/多/改名/换序差异均已在「需求字段→处置」对照表登记(裁剪不阻塞) |
| 字段集与设计双向比对 | ✅ | 实现列/字段集合(scan_field_columns) ⟷ 详细设计「字段实现清单」(scan_design_field_inventory)双向一致, 无"设计有实现无"(如 soldCount 漏渲染)Critical、无"实现有设计无"未登记增列 Important, 展示顺序与设计「顺序」列一致 |
| 视觉还原度（L1/L2·样式一致性·视觉还原支柱②） | ✅ | L1 核心页与视觉基准(高保真 或 原型 code/+设计令牌)布局/控件一致, 主色/字号/间距/圆角均取自原型 token 体系(非组件库默认、非硬编码), L2 无明显非结构偏差 |
| 内容 + 操作逻辑完整性（对齐原型第三层） | ✅ | 原型可见区块/卡片/次级操作/跳转与交互态(loading/empty/error/success/disabled)均已实现, 校验/联动/条件显隐/增删改流/二次确认/步序等操作逻辑照原型未简化未改流程, 未实现项均在原型内容基线登记裁剪/延期 |
| 文案与数据口径一致性 | ✅ | scan_stale_copy 无命中: 表 E 处置=改写行的旧口径字样在改了口径的展示位说明文案(tooltip/表头注解等)中无残留(口径由「仅企业」扩为「企业+个人」后 6 处 tooltip 全部同步改写), 表 F 作废行的 REQ 编号在代码注释中均已标注「该需求已于 {version} 作废」 |
| 叶子组件替换白名单（约定28 实现选型层） | ✅ | 原型 Antd 等叶子小组件(Tag/下拉/日期选择/表格/分页/Card 等)换成项目 Element Plus 等价件, 且主题 token 取自原型体系(非组件库默认)、操作逻辑/交互行为与原型一致; 白名单外的结构/版心/栅格/内容/操作逻辑均未偏离 |
| 死代码/死引用残留 | ✅ | scan_code_conventions dead-ref 0 命中, 无引用已删旧组件/路由 |
| DB 约束前置校验 | ✅ | 设计 A.3 各约束(NOT NULL/length/UNIQUE/外键)在 Controller/Service 有前置校验 |
| HTTP 客户端配置硬编码 | ✅ | http-hardcode 0 命中, base URL/IP/凭证走配置 + ${ENV} 占位 |
| 配置中心热刷新漏标 | ✅ | 可变 key 的 @Value 类均带 @RefreshScope / @ConfigurationProperties |
| 新增依赖/import 越界基线 | ✅ | dep-baseline 0 命中, 新增 import 顶层包均已声明在清单 |
| 审计字段填充溯源 | ✅ | audit-fill 0 命中, 审计字段走自动填充从登录态取操作人, 无写死 system, 逻辑删除同步 update_by/update_time |
| 用户自我保护校验 | ✅ | 用户删除/禁用/降权接口后端以当前登录态身份(JWT sub/SecurityContext)为基准拦截自伤操作, 非仅前端按钮置灰 |

### 4b. 实现偏离设计（**维度 13**·外部脚本门控；未下发脚本时标「不适用」并留行）
| 检查项 | 状态 | 说明 |
|--------|------|------|
| 设计字段/常量落点 | 🟡 | `check_design_anchor.py --version V0.14.0 --json` 发现 1 个未直接命中的 `createTime`;已确认本版设计改为 `createdAt`,设计与相关 Task 已按约定 22 级联修订，并在原锚点处登记豁免理由 |

### 5. DI 依赖可解析性（**维度 6**·Java/Spring；非 Java 项目整维度跳过）
| 检查项 | 状态 | 说明 |
|--------|------|------|
| 注入点可解析性 | ✅ | check_di_resolvability 扫 236 个 .java / 267 个注入点, Critical 0 项——无「注入了框架不自动装配的类型(RestTemplate/WebClient 等)」、无「注入仓内具体类却无组件注解/@Bean」 |
| 多 bean 无 @Qualifier | 🟡 | 4 项 Warn(不阻断): PaymentGateway 有 2 个带注解实现且未 @Qualifier/@Primary, 已确认按字段名匹配 beanName 兜底可解析 |
| @Value 键存在性 | ✅ | 65 个占位符, 无默认值的键均在 application*.yml 命中(relaxed binding 忽略 -/_/大小写), 无缺失 |

### 6. CSS 预处理器一致性（**维度 7**·Vue；非 Vue 项目整维度跳过）
| 检查项 | 状态 | 说明 |
|--------|------|------|
| `<style lang>` 所需预处理器已声明 | ✅ | check_vue_style_preprocessor 扫 48 个 .vue / 41 个 `<style>` 块, ERROR 0 项 |
| 与全库众数 lang 一致 | 🟡 | 2 项 Warn(不阻断): 39 处 scss 中 2 处 less 孤例, `less` 确已声明, 经确认为有意混用 |

### 7. 请求通道 URL 拼装（**维度 8**·前端；无前端源码的项目整维度跳过）
| 检查项 | 状态 | 说明 |
|--------|------|------|
| base 拼装判据单一信源 | ✅ | check_request_channel_url 扫 124 个前端文件, 计入自拼点 1 处、集中于 `src/utils/apiUrl.ts`, 认定为单一信源（**本行为填表示例，数字非实测**）|
| 非请求通道用法（不计入） | ℹ️ | 1 处: `stores/auth/user.js:51` 用 base 拼 `location.href` 做页面跳转, 所在文件无请求发起点 |

### 8. UI 还原度确定性检查（**维度 10**·约定39-R2/R3/R10；被测项目无 `check_ui_fidelity.py` 时标「不适用」跳过、**仍留行**）
| 规则 | 状态 | 说明 |
|--------|------|------|
| 约定39-R10 导出透传分页参数 | ✅ | `check_ui_fidelity.py --json` findings 中 R10 = 0 项（**零豁免规则**，命中即 Critical） |
| 约定39-R3 截断无 tooltip | 🟡 | 2 项 Important（不阻断）：均已就近声明 `fidelity-ignore: R3 装饰性副标题，设计上不给 tooltip` |
| 约定39-R2 状态标签颜色字面量 | ✅ | 0 项 |

### 9. 失效实体与边界（**维度 11**·约定39-R5~R9 + R12·纯静态语义核对）
| 规则 | 状态 | 说明 |
|--------|------|------|
| R5 失效实体入口拦截 | ✅ | 已下架产品在列表渲染处 `v-if="row.status !== 'OFFLINE'"` 收掉详情入口，非点击后报错 |
| R6 被删引用降级展示 | ✅ | 商品名取 `productNameSnapshot ?? '已删除的商品'`，无 code/ID 裸露 |
| R7 加载态无脏数据 | ✅ | 选项列表初始值为 `[]` + 渲染带 `v-if="!loading"` 守卫，无"先给一份假的" |
| R8 编辑后视图同步 | ✅ | 保存成功回调调用 `fetchList()`；详情改名后经 store 回写列表（跨组件场景已核） |
| R9 存量数据兼容 | ✅ | 本轮新增必填 `channel_type` 带 `DEFAULT 'LEGACY'` + 读取侧兜底，老数据路径已核 |
| R12 同一指标跨页面同源 | ✅ | 订单量三处展示均调 `GET /api/stat/overview`，与详细设计「统计指标口径表」权威口径一致 |

### 10. 上游调用日志可见性与脱敏（**维度 12**·约定40；被测项目无 `check_upstream_call_log.py` 或非 JVM 栈时标「不适用」跳过、**仍留行**）
| 判据 | 状态 | 说明 |
|--------|------|------|
| C1 出站调用类零日志 | ✅ | `check_upstream_call_log.py --json`:scanned_files 236 / outbound_files 6,C1 = 0 项 |
| C2 成功路径不可见 | ✅ | 6 个出站客户端成功路径均为 INFO,无「只有 warn/error」的类 |
| I1/I2/I3(Important) | 🟡 | 1 项 I2:`SmsClient` 成功路径仅 debug,已要求提升为 INFO(不阻断) |
| 12.1 脱敏覆盖嵌套结构 | ✅ | 脱敏落在自定义 Serializer,`toJson(request)` 路径上的 `password`/`token` 已被 `@JsonIgnore` 拦下,非只在最外层拼串替换 |
| 12.2 二进制降级只打元信息 | ✅ | `OssClient` 上传路径打 文件名+大小+contentType,未见 base64 / `new String(bytes)` 入日志 |
| 12.3 拦截器覆盖全部出站客户端 | 🟡 | 6 个出站客户端中 HTTP 类 4 个已被 `FeignLogInterceptor` 覆盖;**对象存储与短信 SDK 2 个不在 HTTP 拦截器覆盖面内**,已逐个点名要求自行补日志 |

### 11. 优化建议
1. 【性能】用户列表查询较慢，建议评估缓存方案（需先补充详细设计「缓存方案」子章节并经用户确认，见维度 4 缓存一致性规则）
2. 【代码】UserService 的 validateUser() 可提取为工具类

### 12. 技术风险（维度 5）
- 中风险：用户列表未做分页限制，数据量大时可能影响性能

### 13. 技术债（维度 5）
- 用户详情页使用轮询方案，建议后续改为 WebSocket
```

**输出格式示例（验收不通过）：**

```markdown
## 代码验收结果 ❌ 不通过

**验收范围：** 用户管理模块
**验收轮次：** 第 2 轮

### 问题汇总
- 🔴 Critical：3 项
- 🟡 Important：1 项

---

### 🔴 Critical 问题

**问题 1：用户列表仍在使用 mock 数据**
- **位置：** `src/views/user/UserList.vue:45`
- **问题：** 组件中硬编码了 mockUserList 数组
- **代码：**
  ```javascript
  const mockUserList = [
    { id: 1, name: 'Alice' }
  ];
  ```
- **修复：** 删除 mockUserList，调用 getUserList() API

**问题 2：创建用户接口参数不一致**
- **位置：** `src/api/user.ts:23`
- **问题：** 前端发送 userName，后端接收 username
- **修复：** 将前端参数改为 username

**问题 3：字段集与设计双向比对不一致（设计有实现无 + 实现有设计无）**
- **位置：** `src/views/product/ProductList.vue`（表格列定义）
- **问题：** 详细设计「字段实现清单」列了「销量」列（`soldCount`，顺序 2），前端表格**漏渲染该列**（设计有、实现无 → Critical）；同时前端自造了「累计浏览量」列，详设「字段实现清单」无此列且未登记（实现有、设计无 → Important，此处一并登记）
- **依据：** `scan_design_field_inventory.py`（设计侧）⟷ `scan_field_columns.py`（实现侧）双向比对
- **修复：** 补渲染「销量」列并对齐设计「顺序」；「累计浏览量」列走确认补登记或移除

---

### 🟡 Important 问题

**问题 4：错误处理不完整**
- **位置：** `src/views/user/UserList.vue:67`
- **问题：** 仅处理了 401，未处理 403/500
- **修复：** 补充错误码映射

---

请 Agent1 按优先级修复上述问题，修复完成后重新提交验收。
```
