---

## 客户端 MCP 业务能力的 Task 边界（条件启用）

`client_mcp.enabled` 默认关闭,旧 `webmcp_enabled: true` 或 `webmcp.enabled: true` 只兼容 Web+WebMCP,两旧别名冲突也报错。此处的能力是**应用主动向 AI 提供业务工具**,与 AI 借 Chrome DevTools/Appium/小程序**测试驱动**操控界面两条轴独立;项目 `{{AIDP_HOME}}/mcp.json`/全局 MCP 配置只说明开发工具可用,不能证明产品实现。新旧显式声明在客户端/启停/实现形态上冲突时就地报错并登记待澄清,不静默覆盖。

上游已证实应用自有工具时,按工具/权限/写操作确认/生命周期/错误/审计/调用证据拆必要 Task,每项仍带步骤、验收、AI 指令、依赖与 PRD/原型/设计锚点;Web 端按已有 WebMCP 设计任务落地,非 Web 仅按已证实的应用服务或桥接做 Task,**不生成空 Phase**、不自造统一入口或 Chrome 参数。声明未提供时无 MCP Task;声明启用但实现未取到时标待澄清,不能拿测试驱动配置替代设计。判据见 [`references/flow-task-model.md`](./references/flow-task-model.md),QR 折入既有检查项 1/6/14,14 维计数不变。
name: dev-execution-planner
description: >
  从 PRD、HTML 原型和详细设计方案生成研发执行计划。每个任务包含清晰的执行步骤和验收标准,但不包含代码级细节。
  生成后强制执行 14 维度独立 Agent 检查(始终必查 12 项 + 多用户/多文件模式各 +1),并遵循 12 条核心原则(item 0~11,下游可按"核心原则 N"权威引用)。
  当用户提到生成执行计划、研发计划、开发执行方案、任务拆解、从设计文档生成开发任务、Sprint 任务分解时触发。
---

# 研发执行计划生成器 (Dev Execution Planner)

## ⚠️ 技术栈命令按需加载（重要）

本文件只写**跨技术栈通用**的规则（Task 拆分粒度、EPIC 完整性、依赖关系、验收标准写法、AI 锚点透传）；**语言/框架专有的命令**（脚手架生成、开发期轻量验证、单测、依赖与构建）拆在 `references/stack-*.md`，**按项目技术栈只加载命中的 1~2 份**：

| 技术栈 | 加载 |
| :- | :- |
| Java / Spring | `references/stack-java-spring.md` |
| 前端 Vue / React | `references/stack-vue.md` / `stack-react.md` |
| Node.js / Go / Python | `references/stack-nodejs.md` / `stack-go.md` / `stack-python.md` |

探测方式与「开发期验证 ≠ 发布期构建」铁律详见 `references/stack-index.md`。

## 角色定义

你是一位研发 Tech Lead。你要生成的是**步骤清晰的执行计划**——每个任务说清楚"做什么"和"怎么验收"，但不需要贴代码、SQL 或 JSON。

## 输入要求

| 输入项 | 必要性 | 说明 |
|--------|--------|------|
| PRD 需求文档 | **必需** | 功能规格说明，**任务拆解的主线依据** |
| HTML 原型 | **必需** | 交互原型，**仅作为前端页面布局和交互细节的参考** |
| 详细设计方案 | **必需** | 技术架构、接口定义、数据库设计 |
| 既有项目代码 | 推荐(详细设计含"✅ 沿用已有"接口或第三方对接时**升为必需**) | 现有代码结构;标"沿用"的接口须穿透 Service 实现层核验可用性(见第一步半之二) |

**参考文档:**
- `references/iteration-plan-sample.md` — **标准格式样本(可直接参照其结构)**:完全符合本文档「第二步:任务拆解」规范的完整执行计划样本,演示「EPIC × Sprint × Task」三层模型、AI 执行指令四件套、9 列任务总表、第三方对接「情况 0」Task X.1~X.5 五件套(THIRD_PARTY_MOCK)、SQL 路径透传(Module D)、金额字段类型守恒与待澄清问题清单;生成新计划前应读取该样本对齐章节骨架与 Task 格式(样本中业务与路径为虚构,需替换为真实项目内容)

**信息优先级原则：**

> **PRD 需求文档是任务拆解的第一信源，原型仅作为前端页面交互设计的辅助参考。**
>
> - 样式/文案/UI 细节（字段标签、按钮文案、错误提示、颜色/字号/间距规格、交互规则等） → **以 PRD 为准**（即便与原型冲突）
> - 页面布局、组件结构、交互流转 → **以原型为参考**（PRD 未约束的细节按原型实现）
> - 功能范围、业务规则、校验规则、验收标准 → **以 PRD 为准**
> - 技术实现（技术栈、表结构、接口路径、字段类型、中间件等） → **以详细设计方案为准**（PRD 中的技术描述仅供参考，详细设计可自主调整）
> - 接口定义、数据库设计 → **以详细设计方案为准**
> - 冲突裁决：**样式/业务类 PRD > 原型**，**技术类 详细设计 > PRD > 原型**

---


## ⚠️ 需求规模档位(scale=S/M/L,可选入参 · 缺省 = L 现状全套)

**要解决的问题:** 不分档时**无论需求大小都走满全套**,实测可达「代码:文档 ≈ 9.4:1」——一个只改两个页面的小需求也产出与大版本同规格的文档族,收益递减而成本线性。

**怎么接收:** 编排层(如 `/version`)据 REQ 数 / 页面数 / 是否建表自动判档,把档位随调用传入——**任一形式均可识别**:
- 显式入参 `scale=S` / `scale=M` / `scale=L`(推荐);
- 或自然语言裁剪指令,如「目标产出:task 并入计划」。

**判档参考(编排层定,本 SKILL 不自行推断):** S ≈ 少量 REQ、无建表、页面级小改;M ≈ 中等版本、有建表或跨模块;L ≈ 大版本 / 架构级改动。**入参缺省或识别不到 → 一律回退 L 档(现状全套),老调用方零影响。**

**各档产出规模(本 SKILL):**

| 档位 | 执行计划产出 | 说明 |
| :-: | :- | :- |
| **S** | **task 并入执行计划、不单独分册** | Task 四件套(步骤/验收/AI 指令/依赖)**一项不减**,只是不再按 Phase 拆多文件 |
| **M** | **正常分册**(按 Phase 拆分) | 常规 |
| **L** | **现状全套** | 缺省档 |

### ★档位只伸缩产出规模,**不放松任何 Critical 硬门**(铁律)

- **覆盖率、溯源引用、字段对账、待澄清问题清单、各 Critical 维度**在**任何档位**都逐项做、逐项判——S 档是"少写几册文档",不是"少查几项"。
- 质量检查(QR)照常全维度跑;**任何档位都不得跳过硬门脚本**。
- 档位**写进产出文档的头部元信息**(如 `> 规模档位: S(编排层判定)`),让下游与复核者知道本轮是按哪档产出的——否则"文档为什么这么薄"会被误判成漏做。
- 有疑义时**就高不就低**:拿不准 S 还是 M,按 M 产出。**收敛产出是优化,漏掉内容是缺陷。**

---

## ⚠️ 不中断原则(Critical)

**生成执行计划过程中遇到 PRD/详细设计表述不清/任务边界模糊/验收标准缺失等需要澄清的地方时,严禁中断生成任务。**

正确做法:
1. **就地标注** — 在对应任务/步骤处加 `❓ 待澄清` 标记,**不删除内容**
2. **给出暂行方案** — 由 Agent 基于详细设计上下文/行业惯例推断合理默认,在原位置标注 `🔧 暂行方案(Agent 推断): [方案描述]`
3. **集中汇总** — 全部待澄清项汇总到执行计划末尾的 "**待澄清问题清单**" 章节(单文件模式)或 `99_待澄清问题清单.md` 文件(多文件模式)
4. **继续推进** — 用暂行方案完成本次执行计划,**不阻塞下游编码/测试 SKILL 工作**

**编号规则:**
- 沿用上游 PRD 的 `Q-NNN` 和详细设计的 `D-NNN`(在执行计划中保留原编号,标注"来自 PRD/设计")
- 执行计划**新增**的待澄清项使用 `P-NNN`(Plan Pending)前缀

**何时应当中断:** 用户尚未提供 PRD/详细设计文件(无输入可推断);涉及合规/安全底线决策且无任何参考。

**环境信息缺失的处理(不中断):** 中间件连接信息(endpoint/账号/密码)、CI 凭据、测试库地址等环境信息若用户未提供,**严禁中断**:
- 在对应 Task(如 Task 1.2 第三方中间件配置)的执行步骤中,使用占位符 `{待用户填写: MINIO_ENDPOINT}`、`{待用户填写: REDIS_HOST}` 等占位
- 同步生成 `.env.example` 模板,占位符与待澄清条目双向对应
- 在执行计划末尾"待澄清问题清单"登记 P-NNN(主题:环境信息缺失;影响范围:Task 1.2 等;暂行方案:以占位符填写,用户提供后全局替换;优先级 P0)
- **严禁自行编造**真实地址(如不得直接写 `http://dev-minio.example.com:9000` 等未经用户确认的地址)

**与"职责边界"章节的关系:** 本原则适用于"设计文件存在且基本完备,但某些细节模糊/缺失"的情况(如某个接口的幂等策略未明、某个字段长度未指定)。若详细设计**核心技术链路缺失**(如无 A.3 数据表设计、B.2 接口定义不完整),仍按下文"职责边界"章节规则中断并要求用户补充。

---

## 执行流程

> **三步骨架 + 两个插入步。每步的强制结论留在本节,细则全文见 [`references/flow-execution.md`](./references/flow-execution.md)(按需 Read,勿全量加载)。**

| 步 | 干什么 | 强制结论(骨架级,不可省) |
| :- | :- | :- |
| **第零步:多用户分配判定** | 判单用户 / 多用户模式 | 用户未明确"模块-开发者"分配 → **单用户模式**;多用户模式产物**一律平铺同目录、人名进文件名后缀**(`01_研发执行计划-张三.md`),**严禁按人建二级子目录**(会让拆分一致性与 Task 编号唯一两道闸门整体失效) |
| **第一步:交叉校验** | PRD × 详细设计 × 原型三方比对 | 冲突裁决:**样式/业务类 PRD > 原型**,**技术类 详细设计 > PRD > 原型**;发现缺口登记「待澄清问题清单」,**不中断生成** |
| **第一步半:接口设计检测** | 判详细设计是否含接口契约 | 无接口设计 → 相关 Task 须显式登记依赖,不臆造接口路径与字段 |
| **第一步半之二:接口可用性深度核验** | **Critical** — 标注「✅ 沿用已有」的接口**必须穿透 Service 实现层**验证 | 实测痛点:详设标"沿用"、实现却是 `throw new BizException(501)` 占位,**联调阶段才暴雷**。占位/废弃/外部依赖的接口**必须在 Phase 3 单独拆 Task**;详设未标实现状态时,**主动跑 `check_service_impl_stub.py` + `check_third_party_dep_reverse.py`** 反向兜底并把结果回填「接口可用性核验报告」附录 |
| **第二步:任务拆解** | **EPIC × Sprint × Task 三层模型** | 每个 Task 对应**一个子功能**(非一个模块/一个页面);描述含"等/及/相关/一系列/所有"基本可判粒度过粗、必须再拆;**9 列任务表**,每行**设计锚点 + REQ/PRD 引用 + 原型锚点三锚点同时命中**;**严禁旧版 6 列任务表**(命中即 QR 不通过强制重写);AI 执行指令**严禁简写为「主命令 + "参考设计"」** |
| **第三步:按模板输出** | 按输出格式落盘 | 见下「输出格式」与 `references/iteration-plan-sample.md` 标准样本 |

## 核心原则(12 条 · 结论速查)

> **下面每条都是可执行结论,不可省**;`item 0~11` 是权威编号,下游按「核心原则 N」引用的就是这里。
> **Why / 完整判据 / 正反例 / 拆分清单**见 [`references/flow-task-model.md`](./references/flow-task-model.md) >「核心原则」——**原则 10 / 11 是 Critical,只读结论行拆不出合规 Task,动到它们必须读全文。**

| # | 原则 | 结论 |
| :-: | :- | :- |
| **0** | **零冗余(最高原则)** | Task 中**严禁**复述详细设计已定义的字段/接口/枚举/SQL 约束等技术细节,一律以"参考设计 `<章节+行号>`"代替 |
| **1** | **设计来源必须精确到章节 + 行号范围** | 不接受"参考详细设计"这类模糊引用,必须标注到"`<设计文档路径>` > `A.3 biz_user 表定义` > `L120-L180`"级别,方便执行代码的 Agent 快速定位 |
| **2** | **必须有"AI 执行指令"代码块** | 每个 Task 必须包含一个 `**AI 执行指令**` 小标题及其下的 markdown 代码块 |
| **3** | **AI 执行指令的首行必须是具体命令,不是描述** | 代码块第一行是可直接输入的指令,如 `/dev-logic-architect`、`/项目-api-gen`、`aidp gen:mapper --table biz_user` |
| **4** | **命令选择必须遵循 5 级指令优先级** | L1 AIDP 命令 > L2 项目 `{{AIDP_HOME}}/` 资源 > L3 系统 Claude Code 资源 > L4 通用工具 > L5 Shell 脚本 |  <!-- runtime-path-ignore: 指 Agent 自身目录这一概念，非运行契约路径，两包均保持原样 -->
| **5** | **提示词必须引用前序产出与设计章节** | 提示词必须:(a) 显式引用依赖 Task 的产出;(b) 显式标注设计来源章节和行号;(c) **不复述**设计中已定义的字段/接口/枚举,仅以章节引用代替 |
| **6** | **验收标准必须包含"设计一致性"检查** | 必须有一项验收检查代码与详细设计的一致性(如"字段名/类型/约束与 A.3 章节定义完全一致"),不得全部为功能性检查 |
| **7** | **验收标准必须可自动化** | 至少一项验收标准是可通过命令执行验证的(如静态检查命令、研发自测用例 ID、`/code-verification-loop`;⛔ 不写需要启动服务的 `curl localhost` 类,见约定 35),不得全部为主观判断项 |
| **8** | **产出物路径必须具体** | 不接受"生成用户管理代码"这类模糊产出物,必须精确到文件路径 |
| **9** | **严禁偏离设计** | 若详细设计不明确或有冲突,Task 必须标注"⚠️ 设计待澄清",暂停实施,不允许 Agent 自行推断 |
| **10** 🔴 | **缓存确认 → 独立 Task(Critical)** | **仅当**详细设计 A.2 存在「缓存方案」子章节(用户已确认)才允许拆缓存 Task;**严禁**设计未声明缓存却自作主张拆「加 Redis 缓存」。已确认时须按 **4 类拆独立 Task**,**严禁把缓存逻辑混进业务接口 Task**(拆分清单见分片) |
| **11** 🔴 | **字段处置 → 独立 Task 拆分(Critical)** | 「需求字段→处置对照表」里凡处置**不是「保留」**的字段,执行计划必须**显式承接**,**严禁**默默当保留字段排进常规接口/前端 Task。按处置值分类拆:前端计算还原→独立前端计算 Task;请第三方补→关联第三方对接 Task;裁剪→登记不静默保留(分类全表见分片) |

## 职责边界

执行计划只描述"做什么/怎么验收"，**严禁越界**：

**不应做的事（归其他 SKILL）：**
- ❌ 重新设计接口、字段、数据表（归 `dev-logic-architect`）
- ❌ 编写产品规则、业务逻辑说明（归 `ux-logic-extractor`）
- ❌ 定义技术栈、框架选型（归 `dev-logic-architect` Phase 1）
- ❌ 补充缺失的详细设计内容（归 `dev-logic-architect`）

**发现设计文档不全时：**
- 立即中断执行计划生成
- 在输出开头标注"❌ 详细设计不完备，无法生成执行计划"
- 列出缺失项（如"缺失 A.3 数据表设计"、"B.2 接口定义不完整"）
- 要求用户补充详细设计后重新触发

**执行计划应做的事：**
- ✅ 将详细设计拆解为可执行的 Task
- ✅ 标注每个 Task 的依赖关系、验收标准、AI 执行指令
- ✅ 引用详细设计的章节（零冗余原则）
- ✅ 标注上游溯源（PRD/原型/设计/产品文档/高保真）

---

## 🔗 上游引用规则（Critical 强制要求）

**研发执行计划必须建立完整的"四源引用追溯"体系,每个 Task 必须可反查到详细设计、PRD、原型、(产品需求文档/高保真) 的精确位置。这是下游代码生成 Agent 准确实现的基础。**

### L1 文档级引用(必填,在内容主文档头部声明)

`00_索引.md` 与每个内容文档(`01_研发执行计划.md` 等)头部**必须**包含以下引用块:

```markdown
> **详细设计来源:** `<设计文件路径或目录>` (单文件给文件路径;多文件给根目录)
> **研发 PRD 来源:** `<PRD 文件路径或目录>`
> **原型来源:** `<原型路径或目录>`
> **产品需求文档来源:** `<产品需求文档路径>`(若有)
> **高保真设计来源:** `<高保真设计目录>`(若有)
```

**多用户模式**: 主文档列出所有引用 + 每个开发者子文档同样列出所有引用(便于独立工作时快速定位上游)。

### L2 Phase 级引用(每个 Phase 章节标注本 Phase 的上游来源)

每个 Phase 章节标题下方标注**本 Phase 主要参考**(详细设计 / PRD / 原型三源,不参考的显式写"不参考")。
示例见 [`references/flow-task-model.md`](./references/flow-task-model.md) >「L2 Phase 级引用示例」。

## Post-Generation Quality Review(**强制阶段,不可省 · 细则外置**)

**执行计划生成后,必须派一个独立子 Agent 执行 14 维度质量检查**(始终必查 12 项 + 多用户/多文件模式各 +1),核验三方一致性、完备性、可执行性、上游溯源完备性。

- **执行主体与上下文隔离(Critical):** bash 硬核回检脚本 **+** 14 维度语义审查一律在**独立子 Agent**内完成;主流程**禁止**在当前上下文内联跑脚本或内联逐维度自查,只接收精简结构化报告并决定是否重改,循环至全部通过或达 3 轮上限。
- **落盘后 bash 硬核回检必跑**(子 Agent prompt 步骤 0):`check_task_granularity.py` / `check_doc_split.py --check-tasks` / `check_sql_path_handoff.py`,任一命中即拒绝标记完成。
- **查什么** → [`references/quality-review-checklist.md`](./references/quality-review-checklist.md)(14 维度清单本身)。
- **谁来查、怎么派、跑哪些脚本、退出码怎么读** → [`references/flow-qr-dispatch.md`](./references/flow-qr-dispatch.md)(派发流程全文)。
- **哪些维度由脚本判 / 能否按变更范围裁剪** → checklist 顶部**「维度成本与裁剪索引表」**(调度信息单一信源)与**「QR 变更范围裁剪协议」**。可选入参 **`qr_delta_scope`**,**不传 = 全量**;**机器门一律全量跑**,维度 6(依赖链条)/14(EPIC 完整性)等**全量档不接受裁剪**——环由新旧 Task 共同构成、缺端只有端到端才看得出。
- **报告强制附「QR 执行台账」**:14 行一行不少,**条件维度 10/11 未触发也留行**并写明 `N/A + 原因`;脚本 `skipped:true` 记 `N/A` 不记「通过」。这解决「只拿得到结论、分不清过了还是没跑」。

## 📚 分片索引(细则外置,按需 Read)

> **本 SKILL.md 只保留骨架**:角色 / 输入 / 规模档位 / 核心原则与强制结论 / 执行步骤骨架 / 输出格式 / 约束。
> 下列细则**已整节外置**到 `references/`,**用到哪节读哪节,不要全量加载**——一次调用把全部细则灌进上下文是下游 compact 的主要触发源。

| 分片 | 内容 | 什么时候读 |
| :- | :- | :- |
| [`flow-task-model.md`](./references/flow-task-model.md) | **12 条核心原则全文**(Why/判据/拆分清单)+ 任务模板(AI 执行指令四件套)+ 5 级可用指令清单 + L2 Phase 级引用示例 | 落地任一条核心原则、第二步写 Task 时 |
| [`flow-granularity.md`](./references/flow-granularity.md) | 任务粒度判据、正反例、Task 长示例 | 拿不准 Task 粒度时 |
| [`flow-phase-split.md`](./references/flow-phase-split.md) | Phase 划分规则、第三方接口对接清单模板 | 编排 Phase / 处理第三方对接时 |
| [`task-templates.md`](./references/task-templates.md) | **Task 1.2「第三方中间件配置」的权威完整模板**(执行步骤 / 产出物 / 验收标准,含占位符配置、配置类静态验证);第三方对接任务与 Phase 验证任务两类模板**只在此回指** `flow-phase-split.md` 的权威章节、不重复正文 | 生成 Phase 1 的 Task 1.2(中间件配置)时,由 `flow-phase-split.md` 标为「必读」 |
| [`flow-edge-cases.md`](./references/flow-edge-cases.md) | 各类边界情况处理,尤其「情况 0:第三方未交付」的 Task X.1~X.5 五件套 | 遇到边界情况(尤其第三方未交付)时 |
| [`flow-qr-dispatch.md`](./references/flow-qr-dispatch.md) | QR 派发流程:落盘后 bash 硬核回检命令、独立子 Agent prompt、循环终止条件 | 计划落盘后派 QR 子 Agent 时 |
| [`flow-execution.md`](./references/flow-execution.md) | 执行流程第零步~第三步细则全文 | 走执行流程任一步时 |
| [`quality-review-checklist.md`](./references/quality-review-checklist.md) | **14 维度检查清单本身**(查什么) | QR 子 Agent 逐维核查时 |
| [`output-format.md`](./references/output-format.md) | 完整执行计划骨架、主/子文档结构示例、推荐目录结构、跨子文档引用硬性要求、拆分一致性检查表 | 落盘写文件骨架、拆多文档时 |
| [`iteration-plan-sample.md`](./references/iteration-plan-sample.md) | 标准格式样本(可直接参照其结构) | 生成新计划前对齐章节骨架 |

## 优先级

| P0 | 阻塞性任务，被其他任务依赖或核心主流程 |
|----|----------------------------------------|
| P1 | 重要但不阻塞其他任务 |
| P2 | 体验优化、边缘场景 |

## 约束

1. **禁止复用原型 HTML**，前端基于设计方案的技术栈重新开发
2. **任务原子化**，每个任务可在单次 Agent 会话中完成
3. **依赖显式化**，标注 `依赖: Task X.Y`
4. **测试不可省略**
5. **接口全覆盖**，若详细设计包含后端接口定义或存在接口文档，Phase 3 必须覆盖所有接口，不允许遗漏
6. **强制清除 mock 数据**，若详细设计包含后端接口定义或存在接口文档，Phase 4 前端任务的**第一步必须是**扫描并删除该页面/模块的所有 mock 数据和静态假数据（包括硬编码数组、mock 文件、假数据函数、Mock.js 配置等），然后对接后端真实接口。验收标准必须包含"已删除所有 mock/静态假数据"。**例外:** 第三方平台接口未交付场景允许带 `THIRD_PARTY_MOCK` 标注的临时 mock(协议与清理任务详见 [`references/flow-edge-cases.md`](./references/flow-edge-cases.md) >「情况 0」)
7. **金额字段类型守恒**：若详细设计 A.3 数据表中金额字段为 `BIGINT`(按最小货币单位存储,见 dev-logic-architect「💰 货币金额字段设计规则」），生成对应 Entity/Mapper/DTO/前端类型时**必须**保持整数类型:Java `Long`、Go `int64`、Kotlin `Long`、TypeScript 用 `string` 类型(避免 JS Number 2^53 上限溢出,通过 `@JsonSerialize(ToStringSerializer.class)` 或 `BigInt` 处理)。**严禁**改为 `BigDecimal`/`number`/`float`/`double`。前端展示时由 UI 层除以 10^小数位还原为人类可读金额,不在 Entity/DTO 层做换算

## 输出格式

**固定产出：** 执行计划在 Sprint 排期后必须包含 `## Sprint 并行分组` 机器可读表，固定四列为 `并行组 | Sprint 链（有序，组内串行） | 涉及代码域（glob） | 与其它组的文件重叠`。该表是文件级冲突矩阵的机器可读投影：组内按链串行、组间无硬依赖才并行，glob 用于下游集合求交，重叠逐组两两声明（无重叠写「无」），判不准则合并为同组；缺表时下游默认全串行且不报错。完整模板见 `references/output-format.md`。

**三种输出模式的判定规则（必须即时遵守）:**

- **单用户场景（默认）**: 用户未指定人员-模块分配时,恒产出 `00_索引.md`(专职索引) + `01_研发执行计划.md`(内容:5 个 Phase + 任务依赖图/API 端点/Mock 清除汇总附录,头部必填 5 源溯源块)。
- **多用户分配场景**: 仅当用户**明确给出"模块 A→开发者 X、模块 B→开发者 Y"映射**时触发 → 生成 `00_索引.md`(专职索引:人员分配表+全局任务清单+跨人员依赖) + N 个 `NN_研发执行计划-{开发者姓名}.md` 内容文档(从 `01_` 起,每人仅含本人任务 + 依赖外部任务标注)。
- **按内容量拆分多文档（单/多用户均适用）**: **默认内容集中在 `01_研发执行计划.md` 单份,强烈推荐**;仅当 `01_` 内容>800 行/60KB、或 Task>40、或单 Phase Task>15、或用户明确要求拆分时才拆成 `02_`..、`99_` 待澄清。无论是否拆分,`00_索引.md` 恒产出。

**拆分关键约束（Critical,即时知道）:**

- **目标文件数 3~5 个**(含 `00_索引.md`),宁可 `01_` 内容 800~1200 行也不拆成 7、8 个碎片;仅 Task>60 才允许超 5 个文件。内容文档目标 200~800 行,严禁不足 150 行的碎片(`00_索引.md` 除外)。
- **文件名必带两位数字序号前缀**(`00_`/`01_`/`02_`...,字典序=逻辑序);`00_` 恒给专职索引 `00_索引.md`,内容主文档一律从 `01_研发执行计划.md` 起,`99_` 固定待澄清问题清单;后续序号接上一文档自动累进、不跳号、不嵌套二级序号。**严禁用"补充"/"追加"/"附加"/"扩展"作前缀或中缀**(应直接用 `NN_<业务名>.md` / `NN_Phase名`)。
- **必须生成专职索引** `00_索引.md`(仅索引/全局视图,不展开 Task 详情;必载全部内容文档清单+每份生成时间[shell `date` 打戳]+主/补充标识);按 Phase 拆内容文档优先(`02_`..),Phase 仍过大再按业务域拆。
- **Task 编号全局唯一**,跨主/子文档完全一致、不重编号;Phase 内按依赖顺序从小到大排列。子文档间引用用相对路径 + Task 编号锚点;零冗余原则在拆分后仍生效(以章节引用代替复述设计字段/接口/枚举)。
- **多用户文件命名**: 专职索引 `00_索引.md`,内容文档 `NN_研发执行计划-{开发者姓名}.md`(从 `01_` 起),同目录便于交叉引用。

> 完整执行计划骨架(单用户/多用户主子文档)、主子文档结构示例、推荐目录结构、跨子文档引用硬性要求与拆分一致性检查表详见 [references/output-format.md](./references/output-format.md)

---

## 增量 / 补充生成约定

> 供下游命令(如 `/sprint-dev` 的"口述累进 / 代码完成回头补执行计划")以增量 / 补充方式调用本 SKILL 时遵循。本节只约定增量协作接口,不改变上文任何全量产物的命名与拆分规则。

1. **无独立 mode / 参数开关** — 本 SKILL **没有** `mode=supplement` 之类的模式参数;增量补充完全由**调用方在 prompt 中描述「本次增量范围 + 关联主文档」**驱动(prompt 驱动增量)。严禁臆造、要求或假设一个并不存在的 mode 参数。
2. **增量产物命名续编最大序号** — 增量执行计划产物与全量产物**同目录**,统一 `NN_<Phase名 / 业务主题>.md` 纯数字前缀(两位序号在文件名最前),序号**接已有最大序号自动累进**(不跳号、不嵌套二级序号)。**文件名一律不带「补充」/「追加」/「附加」/「扩展」字样**(遵循上文拆分关键约束);增量身份不写进文件名,而是靠「产物头部回链 + `00_索引.md` 追加一行(类型=补充 + 生成时间[shell `date` 打戳])」体现——`--supplement` 产出时**只给 `00_索引.md` 追加一行**,而非改内容主文档正文。
3. **增量产物头部回链(三类)** — 每个增量产物头部必须回链:① **`00_索引.md`**(专职索引);② **本轮输入来源**(本次增量所依据的 PRD / 详细设计 / 原型 / 口述需求);③ **同轮其他层补充**(如有,如同一轮里其它 SKILL 产出的增量文档,便于跨层对齐)。
4. **增量仍跑 14 维度 QR** — 增量模式下**照常执行**本 SKILL 的 14 维度独立 Agent 质量检查,只是**检查范围限定在本次增量任务 + 与主文档的一致性**(引用 / EPIC 与 Task 编号 / 术语 / 命名 / 上游溯源必须与主文档一致、Task 编号全局唯一不重号),不必重扫未改动的全量产物。
   ⚠️ **「不必重扫」有明确例外,别当成整份产物都不用看**:裁剪的**可执行判据**以 [`references/quality-review-checklist.md`](./references/quality-review-checklist.md) 的**「QR 变更范围裁剪协议」**为准(调用方传 `qr_delta_scope`,不传 = 全量)——**机器门一律全量跑**,标「全量」档的维度(6 依赖链条 / 7 框架中间件 / 10 / 11 / 13 / 14 EPIC 完整性)**不接受裁剪**,因为**它们的失效形态正是「新增这一处与旧的那些不一致」,只看增量内部一个都发现不了**,而增量迭代恰是它们的高发期。本条与该协议冲突时**以协议为准**。

---
