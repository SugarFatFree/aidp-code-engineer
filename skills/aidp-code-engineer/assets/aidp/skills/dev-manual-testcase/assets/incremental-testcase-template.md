# {项目/系统名称} — 研发人工自测用例(增量 — {迭代版本})

> **生成时间**: {YYYY-MM-DD}
> **生成模式**: 增量模式(仅覆盖本次迭代变更 + 关联回归)
> **自测方案来源**: [`01_研发自测方案.md`](./01_研发自测方案.md)(**Critical 必填**,与本用例双向引用)
> **研发执行计划范围**: `{研发执行计划路径}` > {版本/Sprint 编号}
> **PRD 来源**: `{PRD路径}`
> **详细设计**: `{设计路径}`(如有,无则标"无")
> **原型来源**: `{原型路径}`(如有,无则标"无")
> **高保真设计来源**: `{高保真设计目录}`(如有,无则标"无")
> **代码仓库来源**: `{代码仓库根目录}`(已有代码时必填;无则标"无")
> **测试环境**: {URL}

---

## 一、本次迭代变更清单

| Task ID | 变更类型 | 功能点 | 影响页面 | 关联套件 | 关联用例 |
| :- | :-: | :- | :- | :- | :- |
| Task 2.3 | 🆕 新增 | 用户批量导入 | 用户管理页 | SUITE-INC-01 | TC-USER-I01 ~ I03 |
| Task 4.2 | 🐛 修复 | 工单状态切换偶发失败 | 工单详情页 | SUITE-INC-01 | TC-ORDER-I01(原 Bug 复现) |
| Task 3.5 | ✏️ 修改 | 登录密码强度规则 | 修改密码页 | SUITE-INC-01 | TC-PWD-I01 |

**图例**: 🆕 新增 / ✏️ 修改 / 🐛 修复

---

## 二、测试环境与准入

### 2.1 环境信息

| 项目 | 值 |
| :- | :- |
| 测试环境 URL | `{URL}` |
| 客户端类型 | {浏览器/APP} |
| 推荐浏览器 | Chrome ≥ 100 / Edge ≥ 100 |

### 2.2 测试账号(按角色分组)

| 角色 | 账号 | 密码 | 备注 |
| :- | :- | :- | :- |
| 系统管理员 | `admin` | `***` | 全部菜单与操作权限 |

### 2.3 前置数据准备

执行用例前需准备的已有数据（参考全量模式文档"一、1.3 前置数据准备"）

### 2.3.1 前置探测项定义(可选,供浏览器前批量探测)

> 相同实体/状态/探测方式的用例复用同一个 `ED-NNN`;执行内核按编号只探测一次。定义必须包含实体/状态、只读探测方式和可直接写入 `block(precondition-unmet)` detail 的缺位短语。

| 前置项 | 探测实体/状态 | 探测方式（只读） | 不满足时 block 归因 |
| :- | :- | :- | :- |
| ED-001 | 工单，存在 ≥1 条 `status=待处理` | 只读接口 `GET /orders?status=待处理`，按成功响应 `total` 判断 | 缺少 status=待处理 的工单样本 |

> 前置探测必须显式区分 `success=false` / 非 2xx 与成功空结果,并按探测类断言三项自检登记方向、特异性和阳性对照分档。

### 2.3.2 环境能力前置定义(`EC-NNN`,可选;与 `ED-NNN` 同段执行、判据不同)

> `ED-NNN` 问「**样本在不在**」(每轮重探);`EC-NNN` 问「**这个环境提供不提供这个能力**」
> (整轮探一次,不满足时**同一能力的整批用例一次性 block**)。**四列固定、⛔ 不得改列名/列序**——下游按列名读。
> **「能力标识」受控词表 `<类别>:<值>`,类别闭集 = `deploy` / `channel` / `fixture` / `account` / `data`**;
> 自由字符串会**静默失配**,而失配表现与「前置真的满足」完全同形。**无声明 = 无前置**,本节整段可省。

| 前置项 | 能力标识 | 判定方式 | 不满足时 block 归因 |
| :- | :- | :- | :- |
| EC-001 | `deploy:standalone-public` | 环境清单声明「独立实例、exposure=public」 | 当前为共享实例，无 exposure=public 独立实例 |

> ⚠️ 三条不可退让(与下游 `auto-test-runner` 逐字一致):未识别标识回落旧流程(⛔ 既不当满足也不当缺位)/
> 无声明 = 无前置 / 探测本身失败标 `block(env-unavailable)` 而非「能力不具备」。全文见 `references/flow-principles.md`。

### 2.4 跨系统依赖

（如有，参考全量模式文档"一、1.4 跨系统依赖"）

### 2.5 执行规则（重要）

**问题记录优先原则：**

> ⚠️ **关键规则**：自测执行过程中发现 Bug、卡点、异常现象时，**不要立即中断测试去修复问题**。
>
> **正确做法**：
> 1. **先记录问题**：在用例步骤的"实际结果 / 备注"栏标注问题现象（如"❌ 不通过: 2026-05-13 点击保存后页面无响应"）
> 2. **继续执行后续用例**：跳过当前失败步骤，继续测试其他功能点
> 3. **统一修复**：等所有用例执行完毕后，汇总问题清单，再统一修复
>
> **原因**：
> - 避免频繁切换上下文，提高测试效率
> - 完整了解系统问题全貌，避免遗漏关联问题
> - 便于批量修复相同类型的问题
> - 保持测试节奏连贯，减少重复登录/导航操作
>
> **适用对象**：
> - 人工测试人员
> - AI 浏览器工具（Claude Code + chrome-devtools-mcp（推荐）/ Playwright MCP / OpenClaw / browser-use 等）
> - 自动化测试脚本

---

## 三、套件索引

| 套件 ID | 套件标题 | 覆盖角色 | 覆盖内容 | 用例数 | 预计时长 |
| :- | :- | :- | :- | :-: | :-: |
| SUITE-INC-01 | 管理员增量与回归测试 | 管理员 | Task 2.3 / 4.2 / 3.5 + 关联回归 | 6 | 20 分钟 |

---

## 四、测试套件详情

> **步骤验收锚点约定(维度 13-B):** 每个用例步骤的"预期现象"列在断言真实结果时,**应额外标注**对应详细设计验收章节的精确锚点(markdown 链接,严禁纯文本),格式 `(验收: [§C-x.y 章节名](../docs/design/detail/{version}/19_测试方案.md#C-x-y))`,便于 QA 跑用例时双向追溯到设计验收标准。下例已在关键断言步骤示范;纯粹的输入回显步骤无对应设计验收章节,可不标。
>
> **语义断言约定(可选,配合自动化执行):** 关键断言步骤可在"预期现象"后附一行机器可判的语义断言 `断言: <表达式>`(反向用例用 `断言(反向): <表达式>`),供下游 `auto-test-runner` 做确定化判定;断言只用页面可见文案(禁选择器/字段名),DSL 语法见 SKILL `references/test-design-methodology.md`「八之二」节。下例已在部分关键步骤示范(如 `断言:状态由"待处理"变为"处理中"`),非必填、增量附加。
>
> **前置探测约定(可选,供浏览器前批量探测):** 若本用例依赖可通过只读接口/数据面确认的实体状态,在用例元信息中增加 `> **前置探测:** ED-001`,并引用主文档「前置探测项定义」表中唯一且一致的四列定义(前置项 / 探测实体/状态 / 探测方式（只读） / 不满足时 block 归因);相同前置复用同一编号。探测失败不得伪装成合法空结果,具体纪律见 `references/flow-principles.md`。
>
> **环境能力前置约定(可选,供第零步一次性集合运算):** 若本用例依赖的是**环境提供不提供某能力**(部署形态 / 日志通道 / 靶场资产 / 环境级数据形态)而非某条数据样本,在用例元信息中增加 `> **环境能力前置:** EC-001`,并引用 §2.3.2 表中的四列定义。⛔ 不要与 `ED-NNN` 混用。
>
> **步骤级加速标注约定(可选):** **状态准备类**步骤可在**步骤号后**加 `[加速可选]`(形如 `| 1 [加速可选] | 登录 | … |`),提示下游执行端可用 WebMCP 工具调用替代多次 DOM 往返。⚠️ **是提示不是许可**;⚠️⚠️ **`[还原度]` 及一切视觉 / 布局 / 样式断言整条禁走 WebMCP**(下游 `check_result.py` C14 判 Critical),⛔ 不要另发明 `[必须DOM]` 标记。见 `references/flow-principles.md`。
>
> **前置数据编排段约定(可选,跨系统前置时使用):** 若用例前置状态**在被测应用内无法构造**(如"订单中心删商品 → 商户后台看统计"),在用例**测试步骤之前**加一个「**前置数据编排**」段(表列:序号 / 动作类型 / 目标系统 / 具体动作 / 完成判据),动作类型取 `他系统UI操作` / `SQL造删数` / `调对方接口`,目标系统的入口 URL / 账号 / DB 连接登记在 `01_研发自测方案.md` §3 测试环境与准入(§3.1 跨系统依赖环境·数据库 / §3.2 测试账号 / §3.3 前置数据准备);若项目另有外部 `测试环境与账号.md`(aiauto-test 端配置、不由本 SKILL 产出)则以之为准。该段属跨系统造数编排、非被测应用步骤,其 SQL / 接口不受"步骤列禁 SQL / 接口"约束;被测应用自身步骤表仍严禁 SQL / 接口。详见 `references/flow-principles.md` >「用例级『前置数据编排』段」。

### 套件 SUITE-INC-01: 管理员增量与回归测试

> **自测方案章节**: [§3.1 增量范围与通过准则](./01_研发自测方案.md#3-1-增量范围)(**Critical 必填**)
> **PRD 来源**: [§五-3 用户管理 + §五-5 工单详情](../requirements/{version}/研发需求/xxx.md#五-3)
> **原型来源**: [#user-list 用户列表](../prototype/{version}/code/user-list.html#user-list)
> **详细设计来源**: [§B.2 批量导入接口 + §B.5 工单状态机](../docs/design/detail/{version}/03_接口设计.md#B-2)
> **研发执行计划来源**: [Sprint-{NNN} Task 2.3 / 3.5 / 4.2](../docs/plans/{version}/研发执行计划.md#Task-2-3)(增量模式必填)
> **承上启下**: 首套件,从登录开始;按"批量导入 → 导入校验 → 新增回归 → 工单 Bug 回归 → 修改密码"顺序连续执行
> **覆盖功规点**: [五-3](../requirements/{version}/研发需求/xxx.md#五-3)、[五-5](../requirements/{version}/研发需求/xxx.md#五-5)
> **覆盖状态**: 共 6 条用例 → P0×4 / P1×2
> **affects**: modules=[批量导入] pages=[/system/user/import]

#### 套件级前置条件

1. 测试环境 URL:`{URL}`
2. 测试账号:`admin / ***`
3. 前置数据:`assets/test-data/user-import.xlsx`(10 行合法数据)、`assets/test-data/user-import-invalid.xlsx`(第 3 行手机号格式错误、第 5 行用户名重复)、状态为"待处理"的工单一条(编号 `WO-20260512-001`)

#### 入口流程 (Suite Setup) — 套件开始时执行一次

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| S1 | 打开 | Chrome 浏览器 | — | 浏览器窗口启动 |
| S2 | 访问 | 地址栏输入首页 URL | `{URL}` | 跳转到"登录"页 |
| S3 | 输入 | "账号"输入框 | `admin` | 显示 `admin` |
| S4 | 输入 | "密码"输入框 | `***` | 掩码显示 |
| S5 | 点击 | "登录"按钮 | — | 跳转到"工作台",右上角显示"admin" |

> 后续用例从"工作台"开始,通过菜单切换连续执行,**不重复登录**。

#### 用例列表

##### 用例 TC-USER-I01: 批量导入用户成功 — **本次新增**

> **优先级**: P0 | **类型**: 正向 | **测试项**: 批量导入 | **反向**: 否 | **覆盖**: F4
> **PRD 来源**: [§五-3 用户管理 > 批量导入](../requirements/{version}/研发需求/用户管理.md#五-3)
> **研发执行计划来源**: [Task 2.3 用户批量导入](../docs/plans/{version}/研发执行计划.md#Task-2-3)
> **承接前置**: 从工作台开始

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 点击 | 左侧菜单"用户管理" | — | 进入"用户列表"页 |
| 2 | 观察 | 页面右上角工具栏 | — | 在"新增用户"按钮右侧可见"批量导入"按钮(本次迭代新增) |
| 3 | 点击 | "批量导入"按钮 | — | 弹出"批量导入用户"弹窗,包含"下载模板"链接、"上传文件"区域、"导入"按钮(置灰)、"取消"按钮 |
| 4 | 点击 | 弹窗内"下载模板"链接 | — | 浏览器下载 `user-import-template.xlsx` 文件 |
| 5 | 上传 | "上传文件"区域 | `user-import.xlsx` | 文件名显示在上传区,状态为"已上传"绿色勾,"导入"按钮变为可点击 |
| 6 | 点击 | "导入"按钮 | — | 按钮置灰显示"导入中...",弹窗内出现进度条,完成后显示"成功 10 条 / 失败 0 条" `断言:存在文本"成功 10 条 / 失败 0 条"` (验收: [§C-3.1 批量导入](../docs/design/detail/{version}/19_测试方案.md#C-3-1)) |
| 7 | 点击 | "关闭"按钮 | — | 弹窗关闭,列表刷新,新导入的 10 个用户出现在列表顶部 |
| 8 | 刷新 | 浏览器 F5 | — | 列表重新加载,新导入用户仍存在 |

**预期总结**:
- 批量导入功能首次上线,合法 Excel 文件可成功导入 10 条用户
- 数据持久化,刷新后仍存在

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

##### 用例 TC-USER-I02: 批量导入字段校验失败提示 — **本次新增**

> **优先级**: P1 | **类型**: 异常 | **测试项**: 导入校验 | **反向**: 是 | **覆盖**: R1,R2
> **PRD 来源**: [§五-3 > 批量导入异常处理](../requirements/{version}/研发需求/用户管理.md#五-3)
> **研发执行计划来源**: [Task 2.3 用户批量导入](../docs/plans/{version}/研发执行计划.md#Task-2-3)
> **承接前置**: 上一用例 TC-USER-I01 结束于"用户列表"页

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 点击 | "批量导入"按钮 | — | 弹出"批量导入用户"弹窗 |
| 2 | 上传 | "上传文件"区域 | `user-import-invalid.xlsx` | 文件显示"已上传" |
| 3 | 点击 | "导入"按钮 | — | 进度条完成后显示"成功 8 条 / 失败 2 条",下方出现错误明细表格 |
| 4 | 观察 | 错误明细表格 | — | 第一行:"第 3 行 / 手机号 / 手机号格式不正确";第二行:"第 5 行 / 用户名 / 用户名 test_user 已存在" |
| 5 | 点击 | "下载错误明细"按钮 | — | 浏览器下载 `import-errors.xlsx`,内容与表格一致 |
| 6 | 点击 | "关闭"按钮 | — | 弹窗关闭,列表中已新增 8 条成功记录,失败行未导入 |

**预期总结**:
- 包含校验失败行的 Excel,合法行正常导入,失败行展示具体错误且不入库
- 用户可下载错误明细文件以便修正后重新导入

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

##### 用例 TC-USER-I03: 用户列表「列表列完整性」结构用例 — **本次新增/变更列时必配**

> **优先级**: P1 | **类型**: 列表列完整性(维度 17) | **测试项**: 列结构核对 | **反向**: 否 | **覆盖**: F2
> **说明(增量适用条件):** 本次迭代**新增/删除/改序了列表列**(或列表为本次新增)时,该列表页强制配一条「列表列完整性」结构用例;仅测行为(能加载/能展开)不足以发现"漏渲染某列/多出未登记列"。
> **PRD 来源**: [§六、字段规格表 > 用户列表展示字段](../requirements/{version}/研发需求/用户管理.md#六-字段规格)
> **设计来源**: [§B.1 用户列表「字段实现清单」(列名/顺序/数据来源/展示规则/上游出处)](../docs/design/detail/{version}/01_详细设计.md#B-1-字段实现清单)
> **研发执行计划来源**: [Task 2.3 用户批量导入](../docs/plans/{version}/研发执行计划.md#Task-2-3)
> **承接前置**: 上一用例 TC-USER-I02 结束于"用户列表"页

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 核对 | "用户列表"表格表头列序(结构断言) | — | **表格自左至右应依次为「用户名 / 姓名 / 手机号 / 所属部门 / 状态 / 创建时间 / 操作」(期望列序取自详设「字段实现清单」),缺列或多出未登记列即 Fail**(断言:表头列从左到右依次为"用户名/姓名/手机号/所属部门/状态/创建时间/操作") |

**预期总结**:
- **「列表列完整性」结构断言:表格列集合与顺序 = 详设「字段实现清单」的列名+顺序,缺列/多出未登记列即 Fail**(设计 → 用例 → 实现 闭环)

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

##### 用例 TC-USER-R01: [回归] 单个新增用户(关联回归)

> **优先级**: P0 | **类型**: 回归 | **测试项**: 新增 | **反向**: 否 | **覆盖**: F4
> **关联缺陷**: —(无单一关联缺陷,工具栏布局连带回归)
> **回归原因**: Task 2.3 新增"批量导入"按钮可能影响工具栏布局,需回归既有"新增用户"功能
> **说明(标记契约):** 标题旁 `[回归]` 文本标记 + `关联缺陷` 字段供下游 `auto-test-runner` 按 `[回归]` / `关联缺陷:` 文本 glob 跨版本继承复跑;既有 TC-ID `-R01` 后缀保留不动
> **PRD 来源**: [§五-3 用户管理 > 新增用户](../requirements/{version}/研发需求/用户管理.md#五-3)
> **研发执行计划来源**: [Task 2.3 用户批量导入](../docs/plans/{version}/研发执行计划.md#Task-2-3)
> **承接前置**: 上一用例 TC-USER-I02 结束于"用户列表"页

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 点击 | "新增用户"按钮 | — | 弹出"新增用户"弹窗(本次迭代未变更) |
| 2 | 输入 | 弹窗内各必填字段 | (合法用户数据) | 各输入框正常显示输入内容 |
| 3 | 点击 | 弹窗内"确认"按钮 | — | 成功新增,列表出现新行,Toast 提示"新增成功" |

**预期总结**: 新增按钮和弹窗在工具栏增加"批量导入"后仍正常工作

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

##### 用例 TC-ORDER-I01: [回归] 工单状态切换偶发失败 — 原 Bug 复现路径(🐛 修复 / Task 4.2)

> **优先级**: P0 | **类型**: Bug 回归 | **测试项**: 状态流转 | **反向**: 是 | **覆盖**: F7,R9
> **关联缺陷**: Bug #1234(缺陷ID ↔ 本回归用例双向关联;/sprint-bugfix 对每条 P0/P1 缺陷各生成一条 `[回归]` 用例)
> **PRD 来源**: [§五-5 工单详情 > 状态切换](../requirements/{version}/研发需求/工单管理.md#五-5)
> **设计来源**: [§B.5 工单状态机](../docs/design/detail/{version}/03_接口设计.md#B-5)
> **研发执行计划来源**: [Task 4.2 修复 Bug #1234](../docs/plans/{version}/研发执行计划.md#Task-4-2)
> **代码来源**: [修复 commit `{commit-hash}`](../{代码仓库相对路径})(Bug 修复用例必填)
> **承接前置**: 上一用例 TC-USER-R01 结束于"用户列表"页

**Bug 原始描述**: 用户在工单详情页快速连续点击"开始处理"按钮 ≥ 2 次,出现状态切换失败 Toast 但状态仍变更,导致数据不一致。

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 点击 | 左侧菜单"工单管理" | — | 进入"工单列表"页 |
| 2 | 点击 | 列表中编号 `WO-20260512-001` 行的"详情" | — | 跳转到"工单详情"页,状态标签显示"待处理"(灰色) |
| 3 | 点击 | "开始处理"按钮(连续快速点击 5 次) | — | **首次点击**:按钮立即置灰显示"提交中...",其余 4 次点击无响应(按钮已禁用);**3 秒后**:Toast 显示"状态已切换为'处理中'",状态标签变为"处理中"(蓝色),按钮变为"已开始处理"(灰色不可点) `断言:状态由"待处理"变为"处理中"` `断言:元素"已开始处理"置灰` (验收: [§C-5.1 工单状态机防重](../docs/design/detail/{version}/19_测试方案.md#C-5-1)) |
| 4 | 刷新 | 浏览器 F5 | — | 页面重新加载,状态仍为"处理中",按钮显示"已开始处理"(灰色) |
| 5 | 返回 | 浏览器返回按钮 | — | 跳回"工单列表",`WO-20260512-001` 行状态显示"处理中" |

**预期总结**:
- 快速重复点击场景下,按钮防重机制生效,仅触发 1 次状态切换
- 状态切换成功后持久化,刷新与返回均显示一致状态

**回归路径**(防止修复引入新问题):
- TC-ORDER-002(状态切换正常路径)
- TC-ORDER-005(从"处理中" → "已完成"的状态切换)

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

##### 用例 TC-PWD-I01: 新密码强度规则生效(✏️ 修改 / Task 3.5)

> **优先级**: P0 | **类型**: 正向 + 边界 | **测试项**: 密码强度 | **反向**: 是 | **覆盖**: R2
> **PRD 来源**: [§六、字段规格 > 密码字段](../requirements/{version}/研发需求/登录.md#六-字段规格)(新规则:8-20 位,必须包含大小写+数字+特殊字符)
> **研发执行计划来源**: [Task 3.5 密码强度规则](../docs/plans/{version}/研发执行计划.md#Task-3-5)
> **承接前置**: 上一用例 TC-ORDER-I01 结束于"工单列表"页(本用例修改密码成功后强制登出,故安排在套件最后)

**测试步骤**:

| 步骤 | 操作动作 | 操作对象 | 操作数据 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | 点击 | 右上角"用户头像"下拉菜单 | — | 展开下拉菜单,显示"修改密码"、"退出登录" |
| 2 | 点击 | "修改密码" | — | 跳转到"修改密码"页,可见"原密码"、"新密码"、"确认新密码"输入框 |
| 3 | 输入 | "原密码"输入框 | `{当前密码}` | 掩码显示 |
| 4 | 输入 | "新密码"输入框 | `weak` | 输入框下方出现红色提示"密码长度至少 8 位",密码强度条显示"弱(红色)" |
| 5 | 清空 | "新密码"输入框 | — | 红色提示消失 |
| 6 | 输入 | "新密码"输入框 | `Abc12345` | 强度条显示"中(橙色)",下方提示"建议包含特殊字符增强安全性" |
| 7 | 清空 | "新密码"输入框 | — | 强度条变为灰色 |
| 8 | 输入 | "新密码"输入框 | `Abc@12345` | 强度条显示"强(绿色)",无错误提示 |
| 9 | 输入 | "确认新密码"输入框 | `Abc@12345` | 无错误提示,"提交"按钮变为可点击 |
| 10 | 点击 | "提交"按钮 | — | 按钮置灰显示"提交中...",顶部 Toast 提示"密码修改成功,请重新登录",2 秒后跳转到登录页 |

**预期总结**:
- 新密码强度规则生效:必须 ≥ 8 位,弱密码(纯字母/纯数字)被拒绝,强密码(大小写+数字+特殊字符)允许
- 修改成功后强制重新登录(本套件至此结束,无需 Teardown)

**实际结果 / 备注**:
- [ ] 通过 / [ ] 不通过:_________________

---

## 五、跨系统验证清单

| 序号 | 用例 ID | 涉及系统 | 验证入口 | 预期现象 |
| :-: | :- | :- | :- | :- |
| 1 | TC-USER-I01 | (无跨系统) | — | — |
| 2 | TC-PWD-I01 | 邮件 | 测试邮箱 | 修改密码成功后收到"密码已修改"邮件 |

---

## 六、测试结果汇总模板

| 套件 ID | TC-ID | 类型 | 执行人 | 执行时间 | 结果 | 备注 |
| :- | :- | :-: | :- | :- | :-: | :- |
| SUITE-INC-01 | TC-USER-I01 | 增量 | | | ✅/❌ | |
| SUITE-INC-01 | TC-USER-I02 | 增量 | | | ✅/❌ | |
| SUITE-INC-01 | TC-USER-I03 | 增量 | | | ✅/❌ | |
| SUITE-INC-01 | TC-USER-R01 | 回归 | | | ✅/❌ | |
| SUITE-INC-01 | TC-ORDER-I01 | Bug回归 | | | ✅/❌ | |
| SUITE-INC-01 | TC-PWD-I01 | 增量 | | | ✅/❌ | |

---

## 七、问题汇总清单

> **说明**: 测试执行完毕后，将所有标注为"❌ 不通过"的步骤汇总到此表格中，便于统一修复。

| 序号 | 用例 ID | 问题描述 | 严重程度 | 复现步骤 | 状态 |
| :-: | :- | :- | :-: | :- | :-: |
| 1 | TC-USER-I01 | 批量导入时文件格式校验不生效 | S0 | 见 TC-USER-I01 步骤 5 | 待修复 |
| 2 | TC-PWD-I01 | 密码强度提示文案与 PRD 不一致 | S2 | 见 TC-PWD-I01 步骤 4 | 待修复 |

**缺陷严重程度定义**(缺陷分级，非用例优先级；用例优先级仅 P0/P1/P2 三档):
- **S0**: 阻塞性问题，核心功能无法使用，必须立即修复
- **S1**: 重要问题，影响用户体验或业务流程，需优先修复
- **S2**: 一般问题，不影响核心功能，可延后修复
- **S3**: 轻微问题，优化建议，可根据排期决定是否修复

---

## 八、待澄清问题清单

> 单文件模式必有本章节;即使无项也保留标题并标注"无待澄清项"。编号沿用上游 Q-NNN/D-NNN/P-NNN,用例新增项用 T-NNN。

| 序号 | 主题 | 影响范围 | 待澄清问题描述 | 🔧 Agent 暂行方案 | 优先级 | 来源 | 用户确认状态 |
| :- | :- | :- | :- | :- | :-: | :- | :-: |
| (无待澄清项时保留本表头并标注"无待澄清项") | | | | | | | |

---

## 八之二、通用还原度覆盖表(维度 21 强制产出,生成必附)

> 四类页面各配固定模板项(列表页 10 条 / 详情页 4 条 / 表单页 2 类 / 含数字的页 6 条),
> 模板全文见 `references/flow-fidelity-suite.md` 第三节。**页面类型只有四个枚举值**,
> 一页多类**拆成多行**、不在一格塞两个类型;表里声明的每个编号必须真有一条同编号
> `[还原度]` 用例(硬门 `check_fidelity_suite.py` 逐个回查)。

| 页面 | 页面类型 | 还原度用例数 | 用例编号 |
| :- | :- | :-: | :- |
| {页面名} | 列表页 | 10 | TC-{模块}-F01~F10 |
| {页面名} | 详情页 | 4 | TC-{模块}-F11, TC-{模块}-F12, TC-{模块}-F13, TC-{模块}-F14 |
| {页面名} | 表单页 | 2 | TC-{模块}-F15, TC-{模块}-F16 |
| {页面名} | 含数字的页 | 6 | TC-{模块}-F17~F22 |

- 模板项**确实不适用**时(如产品明确不提供导出),数字照实填 + 用例位置就地标 `不适用 + 原因`,⛔ 不得为凑数写假用例。
- 本轮确实没有 UI 页面用例时,写明「**本轮无 UI 页面用例**」+ 原因,硬门据此整档跳过。
- ⚠️ 「复用同一接口、不另算」这类说明写在表下方注里,**不要塞进单元格**。

## 八之三、零残留断言登记表(维度 18 子条,有反向零残留断言即必附·跨版本累积)

> **「零残留断言」** = 一切 `断言(反向):不存在文本"X"` 形态的反向断言,含 `[反向-残留]`(裁剪/延期项
> 该删没删)与 `[文案一致性]` 的反向用例(旧口径字样残留)。它们**只增不退**、成本线性增长且不可逆,
> 故每条都必须在此登记。判据全文见 `references/quality-review-checklist.md` 维度 18
> 「零残留断言登记表」子条;**实跑口径(注释前置)** 见 `references/test-design-methodology.md`
> 「八之二 · 零残留断言的实跑口径」。可用
> `python3 <SKILL_DIR>/scripts/check_residual_assertions.py <用例路径或目录> --json` 机检本表形态。

| # | 断言 | 守护面 | 实跑命令 | 方向一·现状命中 | 方向二·阳性对照 | 退役状态 |
| :- | :- | :- | :- | :- | :- | :- |
| Z-01 | `断言(反向):不存在文本"批量导出"`(TC-USER-R01,裁剪项) | `src/views/user/**;src/components/UserTable*.vue` | `strip_comments.py 'src/views/user/**' \| grep -E '批量导出'`(块级剥离,默认) | 0 命中(2026-09-07 10:12) | 旧「批量导出」按钮原文 `src/views/user/UserList.vue:88`(改前版本) → 1 命中 | 在役 |
| Z-02 | `断言(反向):不存在文本"仅统计企业级"`(TC-STAT-C03,旧口径全站零残留) | `**` | `strip_comments.py '**/*' \| grep -E '仅统计企业级'`(块级剥离,默认;守护面 `**` 涵盖 `.md`,块级剥离对 Markdown 只剥 `<!-- -->`、**不会吞掉列表项与整行加粗**,故无需像退路方案那样分两次跑) | 0 命中(2026-09-07 10:15) | 改前 tooltip 原文 `src/views/stat/Overview.vue:41` 等 3 处 → 3 命中 | 在役 |
| Z-03 | `断言(反向):不存在未标注作废的文本"REQ-0142"`(TC-DOC-C01,判据落在注释上) | `src/**/*.java;src/**/*.ts` | `strip_comments.py --only-comments 'src/**/*.java' 'src/**/*.ts' \| grep -E 'REQ-0142' \| grep -vE '该需求已于 v1\.4 作废'`;**用 `--only-comments` 反过来只扫注释**,理由:本条断言目标本身就是注释内容(失效 REQ 编号的作废标注),按正常方向剥离会把要查的行全剥掉 | 0 命中(2026-09-07 10:18) | 未加作废标注的注释原文 `src/service/OrderService.java:57`(改前版本) → 1 命中 | 在役 |
| Z-04 | `断言(反向):不存在文本"旧版导出"`(原 TC-EXP-R02) | `src/views/export/**` | `strip_comments.py 'src/views/export/**' \| grep -E '旧版导出'`(块级剥离,默认) | — (已退役) | — (已退役,末次 1 命中 @Sprint-045) | 已退役(Sprint-046;守护对象已消失——整个旧导出页面已删除) |

**填写约定(照抄即合规):**

- **守护面**必须由**方向一实跑的命中文件集合导出**,⛔ 不得凭印象手写;语义为「全站不得出现」的
  断言(如 Z-02 旧口径字样)守护面**只能写 `**`**,缩窄它等于把断言变成查不出问题的摆设。
- **实跑命令**写「取材方式 + 式子」。**默认取材方式是块级剥离**
  `python3 <SKILL_DIR>/scripts/strip_comments.py '<守护面>' | grep -E '<式子>'`——它把行级 grep 前置的
  三个漏面(`/* */` 块中间行 / `<!-- -->` 跨行块中间行 / 行尾注释)一次关掉,且不会误吞
  字符串里的 URL 与 Markdown 列表项。**走行级前置退路时必须写明「行级前置」**;
  **改写或关闭取材方式时必须写出完整式子
  + 理由**(见 Z-03),否则与「忘了加」无法区分。
  ⚠️ **前缀集按守护面涵盖的文件类型选、⛔ 不要自行摸索**——固定表见方法论
  「八之二 · 前缀集按守护面涵盖的文件类型选」(`#` / `--` 何时能加、`/\*` 已同时覆盖
  `/*` 与 `/**` 不必另加一条)。下游实测该根因**跨 4 个版本复发 5 次**,
  第 5 次的直接原因就是「项目自己加的排除集漏了一项」。
  ⚠️⚠️ **只有走行级前置退路时**才需要「守护面涵盖 `.md` 就分两次跑、文档面去掉 `\*` 分支」:
  行级默认集里的 `\*` 会把 Markdown 的**列表项 `* 文本`** 与**整行加粗 `**文本**`** 一起静默吞掉,
  方向是**假绿**,而守护面写 `**` 或 `docs/**` 的断言天然涵盖 `.md`、必然中招。
  **块级剥离没有这个问题**(它对 Markdown 只剥 `<!-- -->`),这也是把它定为默认的原因之一。
  ⚠️ **写完整管道时 `|` 必须转义为 `\|`**——GFM 里 `|` 是列分隔符,**反引号代码段保护不了它**,
  不转义会把该行切成 8 格、整张表当场散架(硬门 Z7 专抓这一条)。
- **两个方向都必须有实跑记录**:方向一跑现状、期望 **0 命中**;方向二对着**必然命中的已知样本**跑
  同一条式子、期望 **≥1 命中**。⛔ **方向二留空或写 0 = 这条断言未经验证**——只跑方向一的断言
  可能是「式子写错、永远不会命中」的**恒 0 假绿**,而它在报告里与真正的零残留长得一模一样。
  方向二的样本就是维度 18 要求的「反向复现片段」,**一次实跑同时兑现两条规则,不是两项义务**。
- **退役**只有「守护对象已消失」「判据已移交常设机制(lint / CI grep 门 / 硬门脚本)」两条合法路径,
  且**必须留在表里标 `已退役(轮次 + 依据)`**,⛔ 不许静默删行。
  ⛔ **「跑了很多轮都没红过」不是退役理由**——常绿正是这类断言的正常状态,按它退役等于专挑还在生效的删。
  已退役行的两个方向列填 `— (已退役…)` 即可,硬门对已退役行不再要求实跑记录。
- **⭐ 增量模式下本表是「累积表」,不是「本轮新增表」。** 历史版本登记过的零残留断言**必须逐条抄进来**
  (含已退役行),再追加本轮新增的。零残留断言**只增不退**——下游实测规划期 20 条、两个 Sprint 后 33 条;
  每轮只列本轮新增,等于让历史断言从登记面上**静默消失**,而它们仍在被要求重跑,
  「这条过了」与「这条没跑」在报告里长得一模一样。历史行的实跑记录沿用上一版、无需重跑
  (**本轮跑哪些**属执行计划职责,见 `dev-execution-planner` 轮次收尾验证任务)。
- **本轮确实没有零残留断言时**(既无裁剪/延期项、也无语义口径变更),整节只写一句
  「**本轮无零残留断言**」+ 原因,**不留空表头**。⚠️ 这句话是**写给 QR 子 Agent 看的**——
  `check_residual_assertions.py` **并不识别它**(与还原度覆盖表的「本轮无 UI 页面用例」不同,
  那句有正则专门认);该脚本的跳过条件是「扫描范围里既无零残留断言、也无登记表」,
  写不写这句都照样跳过。写它是为了让人分得清「确认过没有」与「忘了写」。
  ⚠️ 增量模式下这一句极少成立——**累积表里只要还有一条在役历史断言,本节就必须保留整张表**。

## 九、用例统计摘要(完备性度量,生成必附,对应维度 19)

> 增量模式统计范围 = 本次增量用例 + 关联回归用例;口径同全量模式,详见 SKILL `references/test-design-methodology.md` 第六/七节。可用 `python3 <SKILL_DIR>/scripts/check_case_stats.py <用例路径>` 机检。下方为示范骨架。

- 套件总数:1 个;用例总数:6 条(本次新增 + 回归)
- 优先级分布:P0 4 条(67%)/ P1 2 条(33%)/ P2 0 条(0%)
- 正/反向分布:正向 3 条 / 反向 3 条(反向占比 50%,下限 40%;按各用例「反向」字段统计)
- 特殊场景:缓存 0 / Mock 0 / 第三方对接 0 / 并发 0 / 字段逐列核对 1(含 1 条「列表列完整性」结构用例)/ 集成 0
- 数量基线核对:增量集合按变更点规模,不强制套用全量基线;反向占比仍须 ≥ 40%

| 模块/套件 | 用例数 | P0 | P1 | P2 | 正向 | 反向 |
| :- | :-: | :-: | :-: | :-: | :-: | :-: |
| SUITE-INC-01 增量与回归 | 6 | 4 | 2 | 0 | 3 | 3 |
| **合计** | 6 | 4 | 2 | 0 | 3 | 3 |

---

## 附录:AI 浏览器执行入口(可选)

> 默认**本地用 `chrome-devtools-cli` 技能**(免 MCP 配置·免重启;技能名 `chrome-devtools-cli`,**实际命令是 `chrome-devtools`**——自检 `chrome-devtools status`,不存在 `chrome-devtools-cli` 二进制)、**远程用 `chrome-devtools-mcp`**、**优先无头模式**;若使用其他工具替换工具名即可。完整指引见 SKILL `references/chrome-devtools-mcp-setup.md`。

```text
请使用 chrome-devtools-cli 技能(本地,实际命令 chrome-devtools,自检 chrome-devtools status)/ chrome-devtools-mcp(远程)(渲染模式 / 连接模式见自测方案 §2.2)按本文档"四、测试套件详情"逐套件执行用例。

执行约定:
1. 每个套件独立一次会话:先执行套件"入口流程 (Suite Setup)",再按顺序连续执行套件内用例(共享登录态,不重复登录);
2. 关键步骤(登录/提交/状态变化/断言/报错)**必须截图**并对照"预期现象"列验证——**无论有头还是无头,无头无可见窗口、截图是唯一执行证据**;命名主干为 `{用例ID}-step{步骤号}.{ext}`,Web Chrome DevTools 优先 `.webp`(quality=90),不支持 WebP 的驱动保留原生 `.png`,已有驱动的 `.jpg/.jpeg` 仅作兼容。测试用例文档不负责决定驱动支持的格式;每个步骤只登记一个 artifact,不生成多格式副本,真实路径以 auto-test-runner 的 `evidence[].artifact` 为准;
2. 关键步骤(登录/提交/状态变化/断言/报错)**必须截图**并对照"预期现象"列验证——**无论有头还是无头,无头无可见窗口、截图是唯一执行证据**;截图按 用例ID+步骤号 命名归档到测试产物目录;
3. 元素定位优先使用页面可见文案(getByText / getByRole + name);
4. 遇到预期不符不中断套件,记录截图、当前 URL、Toast/弹窗文案后继续下一用例(问题记录优先原则);
5. 在"实际结果 / 备注"列填入"通过 / 不通过 + 说明";
6. 全部套件执行后输出汇总报告(通过率、失败用例清单、典型问题截图)。

测试环境 URL: {URL}
账号/密码: 见"二、测试环境与准入 > 2.2 测试账号"
chrome 调试端点: 本地 http://localhost:9222 / 远程 http://<远程 IP>:9222
渲染模式: 无头模式(默认,无需 GUI)/ 有头模式(仅"被当成真人"反无头 / 测分辨率 / 无头不可用时;有头本地需 GUI,本地无 GUI 则连远程)。本地路径(chrome-devtools-cli)切无头/有头 = 改本机 chrome 的 --headless=new 开关并重启 chrome 进程,Claude Code 无需重启;仅远程路径(chrome-devtools-mcp)首次配置/换远程端点时才需改 MCP 配置 + 重启 Claude Code
单步操作等待上限: 每步 ≤ 3 秒 / 首次打开页面 ≤ 30 秒(默认值,执行前需用户确认);单步超限即判卡死,记录 URL+截图后继续下一步,不无限等待
关键步骤截图: 登录/提交/状态变化/断言/报错等关键步骤必须截图留痕,有头/无头都要求(无头无可见窗口,截图是唯一执行证据);缺截图的关键步骤视为无证据、不予判定通过
```
