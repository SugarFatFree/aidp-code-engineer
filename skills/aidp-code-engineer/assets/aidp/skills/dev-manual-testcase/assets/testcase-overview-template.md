# {项目/系统名称} — 自测用例(总览)

> **文件命名规范:** 本文件默认保存为 `02_自测用例-总览.md`(`00_` 固定保留给专职索引 `00_索引.md`,`01_` 固定保留给 `01_研发自测方案.md`,业务模块用例从 `03_` 开始顺序编号,`98_` 留给跨系统验证清单,`99_` 留给待澄清问题清单)。

> **生成时间**: {YYYY-MM-DD}
> **生成模式**: 全量/增量(按需填写)
> **自测方案来源**: [`01_研发自测方案.md`](./01_研发自测方案.md)(**Critical 必填**,与本用例双向引用)
> **测试环境**: {URL}
> **PRD 来源**: `{PRD路径}`
> **详细设计**: `{设计路径}`(如有,无则标"无")
> **原型来源**: `{原型路径}`(如有,无则标"无")
> **研发执行计划来源**: `{研发执行计划路径}`(增量模式必填,全量模式标"无")
> **高保真设计来源**: `{高保真设计目录}`(如有,无则标"无")
> **代码仓库来源**: `{代码仓库根目录}`(已有代码时必填;无则标"无")

---

## 文档结构与拆分说明

| 序号 | 文件路径 | 内容摘要 | TC-ID 范围 | 用例数 |
| :-: | :- | :- | :- | :-: |
| 03 | `03_登录与权限-自测用例.md` | 登录、注销、修改密码、权限相关 | TC-LOGIN-001 ~ TC-LOGIN-008、TC-PWD-001 ~ 003 | 11 |
| 04 | `04_用户管理-自测用例.md` | 用户增删改查、批量导入、角色绑定 | TC-USER-001 ~ TC-USER-015 | 15 |
| 05 | `05_工单管理-自测用例.md` | 工单创建、流转、详情、状态切换 | TC-ORDER-001 ~ TC-ORDER-012 | 12 |
| 98 | `98_跨系统验证清单.md` | SSO/邮件/企微/支付等跨系统验证汇总 | — | — |
| 99 | `99_待澄清问题清单.md` | T-NNN/Q-NNN/D-NNN/P-NNN 待澄清条目(如有) | — | — |

---

## 一、测试环境与准入(共用,子文档引用此章节)

### 1.1 环境信息

| 项目 | 值 |
| :- | :- |
| 测试环境 URL | `{URL}` |
| 客户端类型 | Chrome 浏览器(≥ 100) |

### 1.2 测试账号(按角色)

| 角色 | 账号 | 密码 | 备注 |
| :- | :- | :- | :- |
| 系统管理员 | `admin` | `***` | 全量权限 |
| 部门管理员 | `dept_mgr` | `***` | 仅本部门 |
| 普通员工 | `user01` | `***` | 仅个人 |

### 1.3 前置数据准备

| 序号 | 数据类型 | 准备说明 |
| :-: | :- | :- |
| 1 | 测试部门 | 已存在"测试部一"、"测试部二" |
| 2 | 测试工单 | 预置 3 条不同状态工单 |
| 3 | 测试文件 | `test-data/user-import.xlsx`、`test-data/avatar.png` |

### 1.4 跨系统依赖

| 系统名称 | 用途 | 测试环境地址 | 验证方式 |
| :- | :- | :- | :- |
| SSO 统一认证 | 单点登录 | `https://sso-test.example.com` | 登录后跳回 |
| 短信网关 | 验证码 | — | 手机收到短信 |
| 企业微信 | 通知 | — | 测试账号企微收消息 |

---

## 二、用例索引(全局)

| TC-ID | 模块 | 用例标题 | 优先级 | 所在子文档 |
| :- | :- | :- | :-: | :- |
| TC-LOGIN-001 | 登录 | 正确账号密码登录成功 | P0 | `03_登录与权限-自测用例.md` |
| TC-LOGIN-002 | 登录 | 密码错误登录失败 | P0 | `03_登录与权限-自测用例.md` |
| TC-USER-001 | 用户管理 | 新增用户成功 | P0 | `04_用户管理-自测用例.md` |
| TC-USER-002 | 用户管理 | 用户名必填校验 | P1 | `04_用户管理-自测用例.md` |
| TC-USER-003 | 用户管理 | 用户列表「列表列完整性」结构用例(列序取自详设「字段实现清单」) | P1 | `04_用户管理-自测用例.md` |
| TC-ORDER-001 | 工单管理 | 创建工单成功 | P0 | `05_工单管理-自测用例.md` |
| ... | ... | ... | ... | ... |

> **📌「列表列完整性」结构用例(维度 17 强制,列表类子文档必含):** 每个**列表 / 表格类页面**在其所属子文档中须有一条「列表列完整性」结构用例(类型标 `列表列完整性`),断言「**表格自左至右应依次为 A / B / C / …,缺列或多出未登记列即 Fail**」,**期望列序取自详细设计的「字段实现清单」**——形成"设计 → 用例 → 实现"闭环,把"详设漏列 / 前端漏渲染 / 实现多出未登记列"变成测试可发现的结构偏差。它测**结构**(该有哪些列、什么顺序),与逐列语义核对(列展示名/顺序/缺多列/处置表一致)并列、互补。

---

## 二之二、通用还原度覆盖表(维度 21 强制产出,生成必附)

> 四类页面各配固定模板项(列表页 10 条 / 详情页 4 条 / 表单页 2 类 / 含数字的页 6 条),
> 模板全文见 `references/flow-fidelity-suite.md` 第三节。**页面类型只有四个枚举值**,
> 一页多类**拆成多行**、不在一格塞两个类型;表里声明的每个编号必须真有一条同编号
> `[还原度]` 用例(硬门 `check_fidelity_suite.py` 逐个回查)。

| 页面 | 页面类型 | 还原度用例数 | 用例编号 |
| :- | :- | :-: | :- |
| {页面名} | 列表页 | 10 | TC-{模块}-F01~F10 |
| {页面名} | 详情页 | 4 | TC-{模块}-F11, TC-{模块}-F12, TC-{模块}-F13, TC-{模块}-F14 |
| {页面名} | 表单页 | 2 | TC-{模块}-F15, TC-{模块}-F16 |
| {页面名} | 含数字的页 | 6 | TC-{模块}-F17~F22 |

- 模板项**确实不适用**时(如产品明确不提供导出),数字照实填 + 用例位置就地标 `不适用 + 原因`,⛔ 不得为凑数写假用例。
- 本轮确实没有 UI 页面用例时,写明「**本轮无 UI 页面用例**」+ 原因,硬门据此整档跳过。
- ⚠️ 「复用同一接口、不另算」这类说明写在表下方注里,**不要塞进单元格**。

## 三、跨系统验证全局汇总

(详细见 `98_跨系统验证清单.md`,此处仅索引)

| TC-ID | 涉及系统 | 验证入口 | 详情链接 |
| :- | :- | :- | :- |
| TC-LOGIN-005 | SSO | `https://sso-test.example.com` | `98_跨系统验证清单.md` > SSO 部分 |
| TC-USER-007 | 企业微信 | 测试账号企微 | `98_跨系统验证清单.md` > 企微部分 |

---

## 四、测试结果汇总模板

| TC-ID | 执行人 | 执行时间 | 结果 | 备注 |
| :- | :- | :- | :-: | :- |
| TC-LOGIN-001 | | | ✅/❌ | |
| TC-LOGIN-002 | | | ✅/❌ | |
| ... | | | | |

---

## 子文档头部规范(每个子文档必须包含)

```markdown
# {模块名} — 自测用例

> **隶属**: 自测用例 > {模块名}
> **总览文档**: [`02_自测用例-总览.md`](./02_自测用例-总览.md)
> **自测方案来源**: [`01_研发自测方案.md`](./01_研发自测方案.md)
> **覆盖范围**: TC-{模块}-{起始编号} ~ TC-{模块}-{结束编号}
> **共用章节**: 测试环境、账号、前置数据见 `02_自测用例-总览.md` "一、测试环境与准入"
```

> ⚠️ **每个含用例的子文档末尾必须各附一节「用例统计摘要」(维度 19)**:`check_case_stats.py` 按**逐文件**核验统计摘要存在性与合计行一致性(本总览文档因只含用例索引、无 `##### 用例 TC-` 用例块,不参与该机检、无须附摘要)。子文档摘要字段同全量模板"八、用例统计摘要",统计范围为本子文档所含用例。

---

## 附录:AI 浏览器执行入口(可选)

> 默认**本地用 `chrome-devtools-cli` 技能**(免 MCP 配置·免重启;技能名 `chrome-devtools-cli`,**实际命令是 `chrome-devtools`**——自检 `chrome-devtools status`,不存在 `chrome-devtools-cli` 二进制)、**远程用 `chrome-devtools-mcp`**、**优先无头模式**;若使用其他工具替换工具名即可。完整指引见 SKILL `references/chrome-devtools-mcp-setup.md`。

```text
请使用 chrome-devtools-cli 技能(本地,实际命令 chrome-devtools,自检 chrome-devtools status)/ chrome-devtools-mcp(远程)(渲染模式 / 连接模式见自测方案 §2.2)按以下顺序执行:
1. 先读取 `02_自测用例-总览.md` 的"一、测试环境与准入"获取环境与账号;
2. 按子文档序号(03_ → 04_ → ...)依次执行各测试套件;
3. 每个套件独立一次会话:先执行套件"入口流程 (Suite Setup)",再连续执行套件内用例(共享登录态,不重复登录),严格按步骤表执行;
4. 关键步骤(登录/提交/状态变化/断言/报错)**必须截图**——**无论有头还是无头,无头无可见窗口、截图是唯一执行证据**;命名主干为 `{用例ID}-step{步骤号}.{ext}`,扩展名由执行适配器决定(Web Chrome DevTools 优先 `.webp` quality=90,其它驱动可为 `.png`,已有 `.jpg/.jpeg` 兼容);每个步骤只登记一个 artifact,不生成多格式副本,真实路径以 auto-test-runner 的 `evidence[].artifact` 为准;
4. 关键步骤(登录/提交/状态变化/断言/报错)**必须截图**——**无论有头还是无头,无头无可见窗口、截图是唯一执行证据**;截图按 用例ID+步骤号 命名归档;
5. 跨系统验证步骤额外参照 `98_跨系统验证清单.md`;
6. 完成后输出全局汇总报告。

测试环境 URL: {URL}
chrome 调试端点: 本地 http://localhost:9222 / 远程 http://<远程 IP>:9222
渲染模式: 无头模式(默认,无需 GUI)/ 有头模式(仅"被当成真人"反无头 / 测分辨率 / 无头不可用时;有头本地需 GUI,本地无 GUI 则连远程)。本地路径(chrome-devtools-cli)切无头/有头 = 改本机 chrome 的 --headless=new 开关并重启 chrome 进程,Claude Code 无需重启;仅远程路径(chrome-devtools-mcp)首次配置/换远程端点时才需改 MCP 配置 + 重启 Claude Code
单步操作等待上限: 每步 ≤ 3 秒 / 首次打开页面 ≤ 30 秒(默认值,执行前需用户确认);单步超限即判卡死,记录 URL+截图后继续下一步,不无限等待
关键步骤截图: 登录/提交/状态变化/断言/报错等关键步骤必须截图留痕,有头/无头都要求(无头无可见窗口,截图是唯一执行证据);缺截图的关键步骤视为无证据、不予判定通过
```
