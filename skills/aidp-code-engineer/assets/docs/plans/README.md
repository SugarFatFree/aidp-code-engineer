# docs/plans/ — 研发执行计划

> **隔离级别**：版本级（不按用户隔离，团队共享）

> ★ **约定 22 攒批级联落点**：`{version}/_开发期计划增量.md`（`_` 起头 → 不占 `NN_` 序号、不进 `00_索引.md`，
> 发布期 `/version` Step 3.3.9.5 合并回 `01_` 主文档）。受 `check_cascade_landing.py` 硬门约束；
> 详规见 `{{AIDP_HOME}}/reference/开发期族增量.md`。

## 目录结构

```
docs/plans/
└── {version}/
    ├── 00_索引.md              # ★ 恒有：专职索引（文件清单 + 每文件生成时间 + 主/补充标识，约定 14/15）
    ├── 01_研发执行计划.md      # 内容主文档（必需）：Sprint 拆分 + 任务分配（历史裸 `研发执行计划.md`/`00_研发执行计划.md` 兼容）
    ├── plan-{user}.md          # 可选：某人承担的任务细化（⚠️ 示例文件名，{user} = git user.name）
    └── plan-{user2}.md         # 可选：多人分工时一人一份
```

## 01_研发执行计划.md 内容

```markdown
# {version} 迭代执行计划

## 版本概览（版本号、里程碑、Sprint 数、预计工期）
## Sprint 拆分
### Sprint-001：{目标}
- 预计时间 / 功能范围 / 前置依赖 / 验收标准
## 任务分配矩阵
| 功能模块 | 后端任务 | 前端任务 | 负责人 | Sprint |
## 依赖关系
## 风险与缓解
```

## plan-{user}.md 内容

某个开发者承担的任务的细化版：
- 任务清单（按 Sprint 分组）
- 执行顺序
- 时间估算
- 风险点

## 生成时机

| 命令 | 产出 |
|------|------|
| `/sprint-plan` | 生成 `00_索引.md` + `01_研发执行计划.md` + 按需生成 `plan-{user}.md` |
| `/version` | 内部调用 `/sprint-plan` |
| `/sprint-dev "<描述>"` | 自动累进 Sprint 时追加行到 `01_研发执行计划.md`，并刷新 `00_索引.md` 生成时间 |
| `/sprint-bugfix "<描述>"` | 自动累进 Sprint 时追加行到 `01_研发执行计划.md`，并刷新 `00_索引.md` 生成时间 |

## 相关文档

- [`../init/02_迭代输入指导.md`](../init/02_迭代输入指导.md) — 第 2 节版本规划期
- [`../init/05_commands详细规范.md`](../init/05_commands详细规范.md)
