# 技术上下文

> **项目级，跨版本跨用户共享。**
> 最后更新：{{date}}

## 项目代码结构

### 前端项目
- **位置**：code/frontend/{子项目}/（已有项目保留原位置时写实际路径）
- **框架**：
- **构建工具**：
- **包管理器**：
- **端口**：

### 后端项目
- **位置**：code/backend/{子项目}/（已有项目保留原位置时写实际路径）
- **框架**：
- **构建工具**：
- **端口**：
- **context-path**：

### SQL 脚本
- **增量轨**：docs/deployment/{version}/sql/增量/NN_中文名.sql（开发期产出，已有环境升级用）
- **全量轨**：docs/deployment/{version}/sql/全量/（发布期产出，从零搭建用）
- 详见 `docs/init/06_版本与用户目录约定.md` §2.5.4

## 运行环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| JDK | >= 17 | |
| Node.js | >= 18 | |
| Maven | >= 3.9 | |
| 数据库 | | |

## 快速启动

### 前端启动
```bash
cd code/frontend/{子项目}/
pnpm install
pnpm dev
```

### 后端启动
```bash
cd code/backend/{子项目}/
mvn spring-boot:run
```

## 环境变量说明

| 变量名 | 示例值 | 是否必须 | 说明 |
|--------|--------|---------|------|
| | | | |

## 外部服务依赖

| 服务 | 用途 | 接入方式 | 配置位置 |
|------|------|---------|---------|
| | | | env/.env |

## 脚手架代码参考

（由 Architect Agent 填充）

## 已知问题和技术债务

- [ ] （待填充）

## 变更历史

| 日期 | 操作人 | 变更内容 |
|------|--------|---------|
| {{date}} | {{user}} | 初始化 |
