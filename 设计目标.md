# 设计目标（Design Goals）— AIDP 主要命令的最终目标

> 仅存在于模板项目 `aidp-code-engineer` 自身，**不随脚手架下发**（根级 `.md` 不在下发面内；⛔ 也别把它挪进 `docs/<新目录>/` 并配 `README.md`，那会被 docs README 镜像器捞进 bundle 真下发）。

---

## 一、这份文件是什么

**每条命令"最终要交付什么"，仅此而已。**

它给审计补上一条别处没有的轴：结构性检查、版本规划产物检查、各确定性脚本门都不回答「**实现是否仍在服务它自己声明的目标**」——本文件就是那个问题的参照系。

## 二、维护纪律（★ 本文件轻易不得更改）

**目标是稳定的。命令天天改、目标不该跟着动**——如果改命令时发现"目标也得跟着改一下"，那说明写进来的**根本不是目标**，是实现。

**只写「最终要达成什么」。不写过程、不写怎么做到的、不写怎么审、不写为什么当初这么定。**

写每一行前自问一遍：

> **这句话会因为实现细节变化而需要跟着改吗？会 → 它不该进这个文档。**

- **不出现实现名词**：文件名 / 分片名 / flag / 脚本名 / 步骤号 / 字段名一律不写——它们一进来，目标就开始跟着命令一起漂。
- **不写违约信号、不写边界例外**：目标写准了，违约就是它的否命题；例外属实现层，由实现分片各自承载。
- **不复述 SKILL 规则**（约定 21）、**不写版本号与变更历史**（约定 30）。
- **审计方法不在这里**：单一信源 = `.aidp/agents/aidp-compliance.md` 语义维度 4。
- **改动被机器拦**：`python3 .aidp/scripts/check_design_goals.py` 对每条目标做指纹棘轮 + 实现名词体检；改一个字就 ERROR，须显式 `--update-baseline` 重新定基（由人拍板，不由执行体顺手做）。
- **实在要改时**：目标变更由**需求变化**驱动、绝不由实现驱动；**先改本文件、再改实现**。顺序反了就是用实现回头定义目标，本文件即告失效。

## 三、命令设计目标

> 编号 `G-<命令>-<序号>` 为**永久锚点**：审计报告与实现分片按它定位。**已退役的序号不再复用**，故各组编号可以不连续——这是刻意的，让老报告里的编号永远指向同一件事。

### G-AUTOPILOT · `/sprint-autopilot`

> **存在理由**：把「产品扔下 PRD」到「版本可交付」之间的全部人工调度消灭掉。

- **G-AUTOPILOT-1** — 一次挂载（开发 + 测试两条调度）后，**「版本规划 → 全量开发 → 部署 → AI 自动化测试」全程自动跑完**，中途不需要人；测试失败后的自动修复与复测也在其内。
- **G-AUTOPILOT-5** — 相关配置齐备时，**外部协作与交付动作一并自动完成**：CICD 监听至终态、AI 测试报告生成、里程碑通知。
- **G-AUTOPILOT-6** — **停得下来，但必须停得响，而且起得来**：熔断、冻结、阻塞都合法，静默不合法；没有通知渠道时，停下这件事同样留在本地可查的告警记录里；每个停下都有够得着的恢复路径。

### G-VERSION · `/version`

> **存在理由**：把一个版本"该想清楚的事"，在开发动手**之前**一次性想完并落成文档。

- **G-VERSION-1** — 版本开工前，需求 / 详细设计 / 执行计划 / 研发自测**一次产齐**；执行期不再新起一套设计。
- **G-VERSION-2** — 规划产物**经独立审计**才算数（自己写的东西自己顺着读一遍不叫审计）。
- **G-VERSION-3** — 文档之间**不存在单边真相**：代码与四份核心上游文档始终是同一份事实；开发期尚未收口的差异必须可被发现。

### G-BATCH · `/sprint-batch`

> **存在理由**：把「执行计划里的 N 个 Sprint」当作一个不可分割的交付单元跑完。

- **G-BATCH-1** — **计划里的全部 Sprint 一次跑完**，中途不问人。
- **G-BATCH-2** — "一次跑完"指的是**不中途向人征询**，**不是**要挤在一个上下文里跑完。

### G-DEV · `/sprint-dev`

> **存在理由**：把"想要的功能"变成代码，且让文档与代码保持同一份事实。

- **G-DEV-1** — **改动也是需求，不是捷径**：任何实质变更最终都要落进四层文档；允许延迟成文，但欠着的必须**可被发现**、且到期必被清掉。

### G-TEST · `/sprint-test`

> **存在理由**：在不启动任何服务的前提下，把"代码是否满足验收标准"判断到可交付浏览器实测的程度。

- **G-TEST-1** — 验收结论**只靠静态验证**得出：不为验证而启动服务，也不以"跑不起来"为由跳过验收。

### G-CLOSE · `/sprint-close`

> **存在理由**：让每个 Sprint 都有明确的收束点。

- **G-CLOSE-1** — **验收结论与归档产物同时存在**；未归档不得开下一个 Sprint。

### G-REQ · `/sprint-requirements`

> **存在理由**：把产品 PRD 转成研发可执行的需求。

- **G-REQ-1** — **PRD 的每一条都有去处**：不静默丢弃，也不凭空补造 PRD 里没有的功能。
- **G-REQ-2** — **原型是内容基线**：原型上有的，要么实现、要么显式裁剪并留痕。

### G-DESIGN · `/sprint-design`

> **存在理由**：详细设计是"代码怎么写"的唯一权威。

- **G-DESIGN-1** — 设计里的事实**与真实代码一致**，不是凭印象推断出来的。
- **G-DESIGN-2** — 需求提到的每个字段、状态、权限范围，**在设计里都有落点**。

### G-PLAN · `/sprint-plan`

> **存在理由**：执行计划是"活怎么分"的唯一权威。

- **G-PLAN-1** — 每个 Task 的**验收标准可核验**，且与已交付代码同向——不写无法判定真假的话，也不出现"计划要求 A、代码是非 A"却仍判通过。

### G-SELFTEST · `/sprint-selftest`

> **存在理由**：研发自测是规划期**给测试期用的**产物；产出即沉睡，等于没产。

- **G-SELFTEST-2** — 规划期产出的用例，**测试期必须逐项消费并断言**。
- **G-SELFTEST-3** — 环境与账号**一次出现即落库**：这类信息几乎只在对话里出现一次，丢了就得反复问人。

### G-FULL · `/sprint-full`

> **存在理由**：单 Sprint 的一条龙。

- **G-FULL-1** — **串起来 ≠ 少门**：一条龙内各段的前置门，与独立调用时一个不少。

### G-AIAUTO · `/sprint-aiauto-test`

> **存在理由**：让"部署完了到底能不能用"这件事，有确定性的、可复核的答案。

- **G-AIAUTO-3** — **报告如实**：实际执行与报告字段一致；**报告失真比测试失败严重得多**。
- **G-AIAUTO-4** — **无人值守不挂起**：阻塞即标记、留证、继续，最后统一汇总交人。
- **G-AIAUTO-5** — **报告一经定稿即不可变**；**可追溯性优先于整洁**。

### G-BUGFIX · `/sprint-bugfix`

> **存在理由**：让"修 bug"有确定的落点与边界。

- **G-BUGFIX-1** — **已发布的版本不再被塞进新 Sprint**：累进只在当前版本尚未发布时成立。
- **G-BUGFIX-2** — 零散 bug 独立修复、**不制造 Sprint 噪音**，且必有可追溯的记录落点。

### G-CHAIN · 跨命令通则（不隶属任何单个命令）

> **存在理由**：串联与推送发生在**每一对**上下游命令之间，写进任一单命令的目标里都必然漏网。

- **G-CHAIN-1** — **无人值守意图逐级传递到底**，不在任何一层丢失。
- **G-CHAIN-2** — 任何一次代码推送都**知道它的部署终态**；无法获知时显式记录为「未知」及原因，不算欠账但必须可见；**推送 ≠ 交付完成**。

### G-START · `/sprint-start`

> **存在理由**：Sprint 有明确的开始点。

- **G-START-1** — **启动即置位**：Sprint 一开始，本地状态就同步到位。

### G-RELEASE · `/version` 发布路径

> **存在理由**：发布半边的契约密度不低于规划半边，故单列一组。

- **G-RELEASE-1** — **双轨部署基线成对产出**（已有环境怎么升级 / 从零怎么搭），两套绝不叠加执行；零变更的版本同样要产全量。
- **G-RELEASE-2** — 全量基线**与真实环境核对过**，且产出过程**全程只读**。
- **G-RELEASE-3** — **发布期不吞欠账**：清不掉的落进可追溯的欠账，而不是消失。
- **G-RELEASE-4** — **已发布出去的东西不被静默改写**：重新发布须用户显式授权，不由执行体默认裁量。

### G-INIT · 初始化与体检类（`/sprint-init`·`-design`·`-complete`·`/health-check`·`/memory-sync`）

> **存在理由**：这类命令给项目体检、同步记忆。

- **G-INIT-2** — **体检只读不写**：不顺手修改被检查的资产。
- **G-INIT-3** — **同步不吞用户写的内容**。

### G-SCAFFOLD · 脚手架（初始化 / 改建 / 升级）

> **存在理由**：让任何项目以最小代价进入范式，并随范式演进而不失去自己的东西。

- **G-SCAFFOLD-1** — 初始化、改建或升级完成后，**项目即可直接使用**，不需要人再手工补齐结构。
- **G-SCAFFOLD-2** — **升级不丢项目自有内容**：用户填写或修改过的内容要么保留，要么被显式列出并可取回。

### G-AGENT · 多 AI 编码 Agent 兼容

> **存在理由**：范式不绑定某一个 AI 编码工具。

- **G-AGENT-1** — 同一流程在支持的各 AI 编码 Agent 下**行为一致**：换工具不改变交付什么、何时停、停在哪里。

---

## 四、附录：下游初始化后的 Agent 目录结构

> ⚠️ **本节不是目标，是事实记录**，故不进第三节的指纹棘轮（`check_design_goals.py` 只扫第三节的 `G-*` 条目）。
> 它存在的理由：第三节刻意不写实现名词，但「装完到底长什么样」是审计时反复要对的一件事，没有它就得每次去翻实现。

脚手架初始化后，**AIDP 的十个契约目录平铺在当前 Agent 自己的目录下**，不再套一层中间目录——这与各 Agent 的原生发现路径一致（`agents/`、`commands/`、`skills/`、`hooks/` 本就是官方位置，套一层等于谁也发现不了）。

**十个契约目录**（固定，不随 Agent 变）：

```
agents/      commands/    flows/       hooks/       plugins/
reference/   rules/       scripts/     skills/      templates/
```

**按启用的 Agent，它们落在哪里：**

| 启用的 Agent | 契约目录落点 | 该 Agent 自己的其余内容 |
|---|---|---|
| 仅 Claude Code | `.claude/` | `settings.json` 等 |
| Codex 和／或 DeepSeek Harness | `.agents/`（两者共享同一份） | — |
| Claude Code 与其它并存 | `.claude/` 与 `.agents/` **各一份**（内容等价、按各自运行根渲染） | 同上 |

**只做入口适配、不放契约的目录：**

| 目录 | 内容 | 为什么不是契约目录 |
|---|---|---|
| `.codex/` | `skills/`（命令入口）、`config.toml`、`hooks.json` | Codex 不读 `.agents/`，需要在它的官方发现根下生成入口 |
| `.dsh/` | `commands/`、`hooks.json`、`mcp.json` | 同上 |

**三条边界**（审计时最常对的就是这三条）：

1. **运行根不独占该目录**：`.claude/` 里还住着 Agent 自己的 `settings.json`、以及项目自有的 skill 与命令。脚手架只认自己清单里的文件，清单之外一律不碰。
2. **脚手架自身不是契约**：安装器落在 `skills/` 下与公共 SKILL 并列，但不属于运行契约，不参与契约指纹与双包比对。
3. **本地运行态不随运行根走**：锁、日志、告警台账恒在 `memory/` 下，不跟着 Agent 目录跑。

---

*仅供模板项目自身维护使用，不随脚手架下发。相关：[`AGENTS.md`](./AGENTS.md) · [`版本变更历史.md`](./版本变更历史.md)*
